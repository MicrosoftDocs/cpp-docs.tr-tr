---
title: CTreeCtrl sınıfı | Microsoft Docs
ms.custom: ''
ms.date: 11/04/2016
ms.technology:
- cpp-mfc
ms.topic: reference
f1_keywords:
- CTreeCtrl
- AFXCMN/CTreeCtrl
- AFXCMN/CTreeCtrl::CTreeCtrl
- AFXCMN/CTreeCtrl::Create
- AFXCMN/CTreeCtrl::CreateDragImage
- AFXCMN/CTreeCtrl::CreateEx
- AFXCMN/CTreeCtrl::DeleteAllItems
- AFXCMN/CTreeCtrl::DeleteItem
- AFXCMN/CTreeCtrl::EditLabel
- AFXCMN/CTreeCtrl::EndEditLabelNow
- AFXCMN/CTreeCtrl::EnsureVisible
- AFXCMN/CTreeCtrl::Expand
- AFXCMN/CTreeCtrl::GetBkColor
- AFXCMN/CTreeCtrl::GetCheck
- AFXCMN/CTreeCtrl::GetChildItem
- AFXCMN/CTreeCtrl::GetCount
- AFXCMN/CTreeCtrl::GetDropHilightItem
- AFXCMN/CTreeCtrl::GetEditControl
- AFXCMN/CTreeCtrl::GetExtendedStyle
- AFXCMN/CTreeCtrl::GetFirstVisibleItem
- AFXCMN/CTreeCtrl::GetImageList
- AFXCMN/CTreeCtrl::GetIndent
- AFXCMN/CTreeCtrl::GetInsertMarkColor
- AFXCMN/CTreeCtrl::GetItem
- AFXCMN/CTreeCtrl::GetItemData
- AFXCMN/CTreeCtrl::GetItemExpandedImageIndex
- AFXCMN/CTreeCtrl::GetItemHeight
- AFXCMN/CTreeCtrl::GetItemImage
- AFXCMN/CTreeCtrl::GetItemPartRect
- AFXCMN/CTreeCtrl::GetItemRect
- AFXCMN/CTreeCtrl::GetItemState
- AFXCMN/CTreeCtrl::GetItemStateEx
- AFXCMN/CTreeCtrl::GetItemText
- AFXCMN/CTreeCtrl::GetLastVisibleItem
- AFXCMN/CTreeCtrl::GetLineColor
- AFXCMN/CTreeCtrl::GetNextItem
- AFXCMN/CTreeCtrl::GetNextSiblingItem
- AFXCMN/CTreeCtrl::GetNextVisibleItem
- AFXCMN/CTreeCtrl::GetParentItem
- AFXCMN/CTreeCtrl::GetPrevSiblingItem
- AFXCMN/CTreeCtrl::GetPrevVisibleItem
- AFXCMN/CTreeCtrl::GetRootItem
- AFXCMN/CTreeCtrl::GetScrollTime
- AFXCMN/CTreeCtrl::GetSelectedCount
- AFXCMN/CTreeCtrl::GetSelectedItem
- AFXCMN/CTreeCtrl::GetTextColor
- AFXCMN/CTreeCtrl::GetToolTips
- AFXCMN/CTreeCtrl::GetVisibleCount
- AFXCMN/CTreeCtrl::HitTest
- AFXCMN/CTreeCtrl::InsertItem
- AFXCMN/CTreeCtrl::ItemHasChildren
- AFXCMN/CTreeCtrl::MapAccIdToItem
- AFXCMN/CTreeCtrl::MapItemToAccID
- AFXCMN/CTreeCtrl::Select
- AFXCMN/CTreeCtrl::SelectDropTarget
- AFXCMN/CTreeCtrl::SelectItem
- AFXCMN/CTreeCtrl::SelectSetFirstVisible
- AFXCMN/CTreeCtrl::SetAutoscrollInfo
- AFXCMN/CTreeCtrl::SetBkColor
- AFXCMN/CTreeCtrl::SetCheck
- AFXCMN/CTreeCtrl::SetExtendedStyle
- AFXCMN/CTreeCtrl::SetImageList
- AFXCMN/CTreeCtrl::SetIndent
- AFXCMN/CTreeCtrl::SetInsertMark
- AFXCMN/CTreeCtrl::SetInsertMarkColor
- AFXCMN/CTreeCtrl::SetItem
- AFXCMN/CTreeCtrl::SetItemData
- AFXCMN/CTreeCtrl::SetItemExpandedImageIndex
- AFXCMN/CTreeCtrl::SetItemHeight
- AFXCMN/CTreeCtrl::SetItemImage
- AFXCMN/CTreeCtrl::SetItemState
- AFXCMN/CTreeCtrl::SetItemStateEx
- AFXCMN/CTreeCtrl::SetItemText
- AFXCMN/CTreeCtrl::SetLineColor
- AFXCMN/CTreeCtrl::SetScrollTime
- AFXCMN/CTreeCtrl::SetTextColor
- AFXCMN/CTreeCtrl::SetToolTips
- AFXCMN/CTreeCtrl::ShowInfoTip
- AFXCMN/CTreeCtrl::SortChildren
- AFXCMN/CTreeCtrl::SortChildrenCB
dev_langs:
- C++
helpviewer_keywords:
- CTreeCtrl [MFC], CTreeCtrl
- CTreeCtrl [MFC], Create
- CTreeCtrl [MFC], CreateDragImage
- CTreeCtrl [MFC], CreateEx
- CTreeCtrl [MFC], DeleteAllItems
- CTreeCtrl [MFC], DeleteItem
- CTreeCtrl [MFC], EditLabel
- CTreeCtrl [MFC], EndEditLabelNow
- CTreeCtrl [MFC], EnsureVisible
- CTreeCtrl [MFC], Expand
- CTreeCtrl [MFC], GetBkColor
- CTreeCtrl [MFC], GetCheck
- CTreeCtrl [MFC], GetChildItem
- CTreeCtrl [MFC], GetCount
- CTreeCtrl [MFC], GetDropHilightItem
- CTreeCtrl [MFC], GetEditControl
- CTreeCtrl [MFC], GetExtendedStyle
- CTreeCtrl [MFC], GetFirstVisibleItem
- CTreeCtrl [MFC], GetImageList
- CTreeCtrl [MFC], GetIndent
- CTreeCtrl [MFC], GetInsertMarkColor
- CTreeCtrl [MFC], GetItem
- CTreeCtrl [MFC], GetItemData
- CTreeCtrl [MFC], GetItemExpandedImageIndex
- CTreeCtrl [MFC], GetItemHeight
- CTreeCtrl [MFC], GetItemImage
- CTreeCtrl [MFC], GetItemPartRect
- CTreeCtrl [MFC], GetItemRect
- CTreeCtrl [MFC], GetItemState
- CTreeCtrl [MFC], GetItemStateEx
- CTreeCtrl [MFC], GetItemText
- CTreeCtrl [MFC], GetLastVisibleItem
- CTreeCtrl [MFC], GetLineColor
- CTreeCtrl [MFC], GetNextItem
- CTreeCtrl [MFC], GetNextSiblingItem
- CTreeCtrl [MFC], GetNextVisibleItem
- CTreeCtrl [MFC], GetParentItem
- CTreeCtrl [MFC], GetPrevSiblingItem
- CTreeCtrl [MFC], GetPrevVisibleItem
- CTreeCtrl [MFC], GetRootItem
- CTreeCtrl [MFC], GetScrollTime
- CTreeCtrl [MFC], GetSelectedCount
- CTreeCtrl [MFC], GetSelectedItem
- CTreeCtrl [MFC], GetTextColor
- CTreeCtrl [MFC], GetToolTips
- CTreeCtrl [MFC], GetVisibleCount
- CTreeCtrl [MFC], HitTest
- CTreeCtrl [MFC], InsertItem
- CTreeCtrl [MFC], ItemHasChildren
- CTreeCtrl [MFC], MapAccIdToItem
- CTreeCtrl [MFC], MapItemToAccID
- CTreeCtrl [MFC], Select
- CTreeCtrl [MFC], SelectDropTarget
- CTreeCtrl [MFC], SelectItem
- CTreeCtrl [MFC], SelectSetFirstVisible
- CTreeCtrl [MFC], SetAutoscrollInfo
- CTreeCtrl [MFC], SetBkColor
- CTreeCtrl [MFC], SetCheck
- CTreeCtrl [MFC], SetExtendedStyle
- CTreeCtrl [MFC], SetImageList
- CTreeCtrl [MFC], SetIndent
- CTreeCtrl [MFC], SetInsertMark
- CTreeCtrl [MFC], SetInsertMarkColor
- CTreeCtrl [MFC], SetItem
- CTreeCtrl [MFC], SetItemData
- CTreeCtrl [MFC], SetItemExpandedImageIndex
- CTreeCtrl [MFC], SetItemHeight
- CTreeCtrl [MFC], SetItemImage
- CTreeCtrl [MFC], SetItemState
- CTreeCtrl [MFC], SetItemStateEx
- CTreeCtrl [MFC], SetItemText
- CTreeCtrl [MFC], SetLineColor
- CTreeCtrl [MFC], SetScrollTime
- CTreeCtrl [MFC], SetTextColor
- CTreeCtrl [MFC], SetToolTips
- CTreeCtrl [MFC], ShowInfoTip
- CTreeCtrl [MFC], SortChildren
- CTreeCtrl [MFC], SortChildrenCB
ms.assetid: 96e20031-6161-4143-8c12-8d1816c66d90
author: mikeblome
ms.author: mblome
ms.workload:
- cplusplus
ms.openlocfilehash: 44e7afd1362064faa19d013da1ee61d32d03227a
ms.sourcegitcommit: 76b7653ae443a2b8eb1186b789f8503609d6453e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 05/04/2018
---
# <a name="ctreectrl-class"></a>CTreeCtrl sınıfı
Windows ortak ağaç görünümü denetiminin işlevselliğini sağlar.  
  
## <a name="syntax"></a>Sözdizimi  
  
```  
class CTreeCtrl : public CWnd  
```  
  
## <a name="members"></a>Üyeler  
  
### <a name="public-constructors"></a>Ortak Oluşturucular  
  
|Ad|Açıklama|  
|----------|-----------------|  
|[CTreeCtrl::CTreeCtrl](#ctreectrl)|Oluşturan bir `CTreeCtrl` nesnesi.|  
  
### <a name="public-methods"></a>Ortak Yöntemler  
  
|Ad|Açıklama|  
|----------|-----------------|  
|[CTreeCtrl::Create](#create)|Ağaç görünümü denetimi oluşturur ve ona ekler bir `CTreeCtrl` nesnesi.|  
|[CTreeCtrl::CreateDragImage](#createdragimage)|Belirtilen ağaç görünümü öğesi için sürükleyerek bir bit eşlem oluşturur.|  
|[CTreeCtrl::CreateEx](#createex)|Ağaç denetimi ile belirtilen Windows genişletilmiş stilleri oluşturur ve ekler bir `CTreeCtrl` nesnesi.|  
|[CTreeCtrl::DeleteAllItems](#deleteallitems)|Ağaç görünümü denetimindeki tüm öğeleri siler.|  
|[CTreeCtrl::DeleteItem](#deleteitem)|Yeni bir ağaç görünümü denetimi öğesini siler.|  
|[CTreeCtrl::EditLabel](#editlabel)|Bir belirtilen ağaç görünümü öğesi yerinde düzenler.|  
|[CTreeCtrl::EndEditLabelNow](#endeditlabelnow)|Geçerli ağaç görünümü denetimindeki ağaç görünümü öğesinin etiketini düzenleme işlemi iptal eder.|  
|[CTreeCtrl::EnsureVisible](#ensurevisible)|Ağaç görünümü öğesi kendi ağaç görünümü denetiminde görünür olmasını sağlar.|  
|[CTreeCtrl::Expand](#expand)|Genişletir veya daraltır, belirtilen ağaç görünümü öğesinin alt öğeleri.|  
|[CTreeCtrl::GetBkColor](#getbkcolor)|Denetimi geçerli arka plan rengini alır.|  
|[CTreeCtrl::GetCheck](#getcheck)|Ağaç denetim öğesi onay durumunu alır.|  
|[CTreeCtrl::GetChildItem](#getchilditem)|Belirtilen ağaç görünümü öğesinin alt alır.|  
|[CTreeCtrl::GetCount](#getcount)|Ağaç görünümü denetimi ile ilişkili ağaç öğe sayısını alır.|  
|[CTreeCtrl::GetDropHilightItem](#getdrophilightitem)|Sürükle ve bırak işlemi hedef alır.|  
|[CTreeCtrl::GetEditControl](#geteditcontrol)|Belirtilen ağaç görünümü öğesi düzenlemek için kullanılan düzenleme denetimi işleyicisini alır.|  
|[CTreeCtrl::GetExtendedStyle](#getextendedstyle)|Geçerli ağaç görünümü denetimi kullanarak genişletilmiş stili alır.|  
|[CTreeCtrl::GetFirstVisibleItem](#getfirstvisibleitem)|Belirtilen ağaç görünümü öğesinin ilk görünür öğeyi alır.|  
|[CTreeCtrl::GetImageList](#getimagelist)|Ağaç görünümü denetimi ile ilişkili resim listesi işleyicisini alır.|  
|[CTreeCtrl::GetIndent](#getindent)|Üst öğesinden bir ağaç görünümü öğesi uzaklığını (piksel cinsinden) alır.|  
|[CTreeCtrl::GetInsertMarkColor](#getinsertmarkcolor)|Ağaç görünümü ekleme işareti çizmek için kullanılan rengi alır.|  
|[CTreeCtrl::GetItem](#getitem)|Belirtilen ağaç görünümü öğesi özniteliklerini alır.|  
|[CTreeCtrl::GetItemData](#getitemdata)|Bir öğesiyle ilişkilendirilmiş 32-bit uygulamaya özgü değerini döndürür.|  
|[CTreeCtrl::GetItemExpandedImageIndex](#getitemexpandedimageindex)|Belirtilen öğe geçerli ağaç görünümü denetiminin genişletilmiş durumdayken görüntülenecek resim dizinini alır.|  
|[CTreeCtrl::GetItemHeight](#getitemheight)|Ağaç görünümü öğeleri geçerli yüksekliğini alır.|  
|[CTreeCtrl::GetItemImage](#getitemimage)|Bir öğesiyle ilişkili görüntüleri alır.|  
|[CTreeCtrl::GetItemPartRect](#getitempartrect)|Geçerli ağaç görünümü Denetim belirtilen öğesinde belirtilen bir kısmı için sınırlayıcı dikdörtgenini alır.|  
|[CTreeCtrl::GetItemRect](#getitemrect)|Ağaç görünümü öğesinin sınırlayıcı dikdörtgenini alır.|  
|[CTreeCtrl::GetItemState](#getitemstate)|Bir öğenin durumunu verir.|  
|[CTreeCtrl::GetItemStateEx](#getitemstateex)|Belirtilen öğe geçerli ağaç görünümü denetiminde genişletilmiş durumunu alır.|  
|[CTreeCtrl::GetItemText](#getitemtext)|Bir öğenin metnini döndürür.|  
|[CTreeCtrl::GetLastVisibleItem](#getlastvisibleitem)|Geçerli ağaç görünümü denetiminde son genişletilmiş öğeyi alır.|  
|[CTreeCtrl::GetLineColor](#getlinecolor)|Ağaç görünümü denetiminin geçerli çizgi rengini alır.|  
|[CTreeCtrl::GetNextItem](#getnextitem)|Belirtilen bir ilişki eşleşen sonraki ağaç görünümü öğesi alır.|  
|[CTreeCtrl::GetNextSiblingItem](#getnextsiblingitem)|Belirtilen ağaç görünümü öğesi sonraki eşdüzey alır.|  
|[CTreeCtrl::GetNextVisibleItem](#getnextvisibleitem)|Belirtilen ağaç görünümü öğesinin sonraki görünür öğeyi alır.|  
|[CTreeCtrl::GetParentItem](#getparentitem)|Belirtilen ağaç görünümü öğesi üst alır.|  
|[CTreeCtrl::GetPrevSiblingItem](#getprevsiblingitem)|Belirtilen ağaç görünümü öğesi önceki eşdüzey alır.|  
|[CTreeCtrl::GetPrevVisibleItem](#getprevvisibleitem)|Belirtilen ağaç görünümü öğesinin önceki görünür öğeyi alır.|  
|[CTreeCtrl::GetRootItem](#getrootitem)|Belirtilen ağaç görünümü öğesi kökünü alır.|  
|[CTreeCtrl::GetScrollTime](#getscrolltime)|Ağaç görünümü denetimi için en fazla kaydırma zaman alır.|  
|[CTreeCtrl::GetSelectedCount](#getselectedcount)|Geçerli ağaç görünümü denetimindeki seçili öğe sayısını alır.|  
|[CTreeCtrl::GetSelectedItem](#getselecteditem)|Şu anda seçili ağaç görünümü öğesini alır.|  
|[CTreeCtrl::GetTextColor](#gettextcolor)|Denetimin geçerli metin rengini alır.|  
|[CTreeCtrl::GetToolTips](#gettooltips)|Alt ağaç görünümü denetimi tarafından kullanılan araç ipucu denetimi için tanıtıcı alır.|  
|[CTreeCtrl::GetVisibleCount](#getvisiblecount)|Ağaç görünümü denetimi ile ilişkili öğeleri görünür ağacı sayısını alır.|  
|[CTreeCtrl::HitTest](#hittest)|İlişkili imleci geçerli konumunu döndürür `CTreeCtrl` nesnesi.|  
|[CTreeCtrl::InsertItem](#insertitem)|Ağaç görünümü denetiminde yeni bir öğe ekler.|  
|[CTreeCtrl::ItemHasChildren](#itemhaschildren)|Belirtilen öğenin alt öğeleri varsa sıfır olmayan bir değer döndürür.|  
|[CTreeCtrl::MapAccIdToItem](#mapaccidtoitem)|Geçerli ağaç görünümü denetimindeki ağaç görünümü öğesine işlenecek belirtilen erişilebilirlik tanımlayıcı eşler.|  
|[CTreeCtrl::MapItemToAccID](#mapitemtoaccid)|Belirtilen tanıtıcı erişilebilirlik tanıtıcısı geçerli ağaç görünümü denetimine ağaç görünümü öğesinde eşler.|  
|[CTreeCtrl::Select](#select)|Seçer, görünüme gelene veya belirtilen ağaç görünümü öğesi yeniden çizer.|  
|[CTreeCtrl::SelectDropTarget](#selectdroptarget)|Ağaç öğesi bir Sürükle ve bırak işlemi hedefi olarak yeniden çizer.|  
|[CTreeCtrl::SelectItem](#selectitem)|Belirtilen ağaç görünümü öğesi seçer.|  
|[CTreeCtrl::SelectSetFirstVisible](#selectsetfirstvisible)|Belirtilen ağaç görünümü öğesi ilk görünür öğeyi seçer.|  
|[CTreeCtrl::SetAutoscrollInfo](#setautoscrollinfo)|Geçerli ağaç görünümü denetim otomatik kaydırma oranını ayarlar.|  
|[CTreeCtrl::SetBkColor](#setbkcolor)|Denetim arka plan rengini belirler.|  
|[CTreeCtrl::SetCheck](#setcheck)|Ağaç denetim öğesi onay durumunu ayarlar.|  
|[CTreeCtrl::SetExtendedStyle](#setextendedstyle)|Geçerli ağaç görünümü denetimi için genişletilmiş stiller ayarlar.|  
|[CTreeCtrl::SetImageList](#setimagelist)|Ağaç görünümü denetimi ile ilişkili resim listesi tanıtıcısı ayarlar.|  
|[CTreeCtrl::SetIndent](#setindent)|Üst öğesinden bir ağaç görünümü öğesi uzaklığını (piksel cinsinden) ayarlar.|  
|[CTreeCtrl::SetInsertMark](#setinsertmark)|Ağaç görünümü denetiminde ekleme işareti ayarlar.|  
|[CTreeCtrl::SetInsertMarkColor](#setinsertmarkcolor)|Ağaç görünümü ekleme işareti çizmek için kullanılan rengi belirler.|  
|[CTreeCtrl::SetItem](#setitem)|Belirtilen ağaç görünümü öğesi özniteliklerini ayarlar.|  
|[CTreeCtrl::SetItemData](#setitemdata)|Bir öğesiyle ilişkili 32-bit uygulamaya özgü değeri ayarlar.|  
|[CTreeCtrl::SetItemExpandedImageIndex](#setitemexpandedimageindex)|Belirtilen öğe geçerli ağaç görünümü denetiminin genişletilmiş durumdayken görüntülenecek resim dizinini ayarlar.|  
|[CTreeCtrl::SetItemHeight](#setitemheight)|Ağaç yüksekliğini öğeleri görüntüle ayarlar.|  
|[CTreeCtrl::SetItemImage](#setitemimage)|Görüntüleri bir öğesiyle ilişkilendirir.|  
|[CTreeCtrl::SetItemState](#setitemstate)|Bir öğenin durumunu ayarlar.|  
|[CTreeCtrl::SetItemStateEx](#setitemstateex)|Belirtilen öğenin Genişletilmiş durum geçerli ağaç görünümü denetiminde ayarlar.|  
|[CTreeCtrl::SetItemText](#setitemtext)|Bir öğenin metnini ayarlar.|  
|[CTreeCtrl::SetLineColor](#setlinecolor)|Ağaç görünümü denetiminin geçerli çizgi rengini belirler.|  
|[CTreeCtrl::SetScrollTime](#setscrolltime)|Ağaç görünümü denetimi için en fazla kaydırma süreyi ayarlar.|  
|[CTreeCtrl::SetTextColor](#settextcolor)|Denetimin metin rengini belirler.|  
|[CTreeCtrl::SetToolTips](#settooltips)|Ağaç görünümü denetimin alt araç ipucu denetimi ayarlar.|  
|[CTreeCtrl::ShowInfoTip](#showinfotip)|Belirtilen öğe için bilgi ipucu geçerli ağaç görünümü denetiminde görüntüler.|  
|[CTreeCtrl::SortChildren](#sortchildren)|Belirtilen üst öğenin alt sıralar.|  
|[CTreeCtrl::SortChildrenCB](#sortchildrencb)|Bir uygulama tanımlı sıralama işlevi kullanarak verilen ana öğesi alt sıralar.|  
  
## <a name="remarks"></a>Açıklamalar  
 "Ağaç görünümü denetimi" bir disk üzerinde bir dizin veya dosya ve dizinleri girdileri olan bir belgeyi başlıklarında gibi öğelerini hiyerarşik bir listesini görüntüleyen bir penceredir. Her öğe bir etiket ve isteğe bağlı eşlemli görüntüyü oluşur ve her bir öğeyi kendisiyle ilişkili alt öğeler listesi olabilir. Kullanıcı, bir öğeyi tıklatarak genişletme ve daraltma ilişkili alt öğeler listesi.  
  
 Bu denetim (ve bu nedenle `CTreeCtrl` sınıfı) yalnızca Windows 98 ve Windows NT sürüm 4 altında çalışan programları için kullanılabilir ve üzerinde desteklenir.  
  
 Kullanma hakkında daha fazla bilgi için `CTreeCtrl`, bkz:  
  
- [Denetimler](../../mfc/controls-mfc.md)  
  
- [CTreeCtrl Kullanma](../../mfc/using-ctreectrl.md)  
  
- [Ağaç görünümü denetim başvurusu](http://msdn.microsoft.com/library/windows/desktop/bb759988) Windows SDK.  
  
-   Bilgi Bankası makalesi Q222905: nasıl yapılır: CTreeCtrl için bir bağlam menüsü görüntüleme  
  
## <a name="inheritance-hierarchy"></a>Devralma Hiyerarşisi  
 [CObject](../../mfc/reference/cobject-class.md)  
  
 [CCmdTarget](../../mfc/reference/ccmdtarget-class.md)  
  
 [CWnd](../../mfc/reference/cwnd-class.md)  
  
 `CTreeCtrl`  
  
## <a name="requirements"></a>Gereksinimler  
 **Başlık:** afxcmn.h  
  
##  <a name="create"></a>  CTreeCtrl::Create  
 Ağaç denetimi bir iletişim kutusu şablonunda belirtirseniz, ya da kullanıyorsanız [CTreeView](../../mfc/reference/ctreeview-class.md), iletişim kutusu veya Görünüm oluşturulduğunda, ağaç denetimi otomatik olarak oluşturulur.  
  
```  
virtual BOOL Create(
    DWORD dwStyle,  
    const RECT& rect,  
    CWnd* pParentWnd,  
    UINT nID);
```  
  
### <a name="parameters"></a>Parametreler  
 `dwStyle`  
 Ağaç görünümü denetimin stilini belirtir. Pencere stilleri, açıklanan uygulamak [CreateWindow'u](http://msdn.microsoft.com/library/windows/desktop/ms632679)ve herhangi bir bileşimini [ağaç görünümü denetim stilleri](http://msdn.microsoft.com/library/windows/desktop/bb760013) Windows SDK'ın açıklandığı gibi.  
  
 `rect`  
 Ağaç görünümü denetimin boyutunu ve konumunu belirtir. Ya da olabilir bir [CRect](../../atl-mfc-shared/reference/crect-class.md) nesnesi veya bir [RECT](http://msdn.microsoft.com/library/windows/desktop/dd162897) yapısı.  
  
 `pParentWnd`  
 Ağaç görünümü denetimin ana penceresinde, genellikle belirten bir `CDialog`. Değil olmalıdır **NULL**.  
  
 `nID`  
 Ağaç görünümü denetiminin kimliğini belirtir.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başlatma başarılı olduğunda sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 Ağaç denetimi alt pencere başka bir pencere olarak oluşturmak istiyorsanız, kullanmak **oluşturma** üye işlevi. Ağaç denetimi kullanarak oluşturursanız **oluşturma**, geçmesi gereken **ws_vısıble**, diğer ağaç görünümü stilleri yanı sıra.  
  
 Oluşturmak bir `CTreeCtrl` iki adımda. İlk çağrıda Oluşturucusu'ı çağırın **oluşturma**, ağaç görünümü denetiminin oluşturur ve ekler `CTreeCtrl` nesnesi.  
  
 Ağaç denetimi ile genişletilmiş pencere stilleri oluşturmak için arama [CreateEx](#createex) yerine **oluşturma**.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_1.cpp)]  
  
##  <a name="createex"></a>  CTreeCtrl::CreateEx  
 Bir denetim (alt pencere) oluşturmak ve bunu ile ilişkilendirmek için bu işlevi çağırmak `CTreeCtrl` nesnesi.  
  
```  
virtual BOOL CreateEx(
    DWORD dwExStyle,  
    DWORD dwStyle,  
    const RECT& rect,  
    CWnd* pParentWnd,  
    UINT nID);
```  
  
### <a name="parameters"></a>Parametreler  
 `dwExStyle`  
 Oluşturulan denetim genişletilmiş stilini belirtir. Genişletilmiş Windows stilleri listesi için bkz: `dwExStyle` parametresi için [CreateWindowEx](http://msdn.microsoft.com/library/windows/desktop/ms632680) Windows SDK'sındaki.  
  
 `dwStyle`  
 Ağaç görünümü denetimin stilini belirtir. Pencere stilleri, açıklanan uygulamak [CreateWindow'u](http://msdn.microsoft.com/library/windows/desktop/ms632679)ve herhangi bir bileşimini [ağaç görünümü denetim stilleri](http://msdn.microsoft.com/library/windows/desktop/bb760013) Windows SDK'ın açıklandığı gibi.  
  
 `rect`  
 Bir başvuru bir [RECT](http://msdn.microsoft.com/library/windows/desktop/dd162897) boyutunu ve konumunu, istemci koordinatları oluşturulacak penceresinin açıklayan yapısı `pParentWnd`.  
  
 `pParentWnd`  
 Denetimin üst penceresi için bir işaretçi.  
  
 `nID`  
 Denetimin alt pencere kimliği  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır değilse 0.  
  
### <a name="remarks"></a>Açıklamalar  
 Kullanım `CreateEx` yerine [oluşturma](#create) Windows genişletilmiş stili önsöz tarafından belirtilen Genişletilmiş Windows stillerini uygulamak için **WS_EX_**.  
  
##  <a name="createdragimage"></a>  CTreeCtrl::CreateDragImage  
 Verilen öğe için sürükleyerek bir bit eşlem bir ağaç görünümü denetiminde oluşturmak, bit eşlem resim listesi oluşturun ve bit eşlem resim listesine eklemek için bu işlevini çağırın.  
  
```  
CImageList* CreateDragImage(HTREEITEM hItem);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Sürüklenen için ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Sürükleme bitmap, başarılı olursa eklendiği resim listesi işaretçi; Aksi takdirde **NULL**.  
  
### <a name="remarks"></a>Açıklamalar  
 Bir uygulama öğe sürüklendiğinde görüntüyü için resim listesi işlevleri kullanır.  
  
 `CImageList` Nesne kalıcıdır ve bittiğinde silmeniz gerekir. Örneğin:  
  
 [!code-cpp[NVC_MFC_CTreeCtrl#2](../../mfc/reference/codesnippet/cpp/ctreectrl-class_2.cpp)]  
  
##  <a name="ctreectrl"></a>  CTreeCtrl::CTreeCtrl  
 Oluşturan bir `CTreeCtrl` nesnesi.  
  
```  
CTreeCtrl();
```  
  
##  <a name="deleteallitems"></a>  CTreeCtrl::DeleteAllItems  
 Ağaç görünümü denetiminden tüm öğeleri silmek için bu işlevini çağırın.  
  
```  
BOOL DeleteAllItems();
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#3](../../mfc/reference/codesnippet/cpp/ctreectrl-class_3.cpp)]  
  
##  <a name="deleteitem"></a>  CTreeCtrl::DeleteItem  
 Ağaç görünümü denetiminden bir öğeyi silmek için bu işlevini çağırın.  
  
```  
BOOL DeleteItem(HTREEITEM hItem);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi silinecek tanıtıcısı. Varsa *hitem* sahip **TVI_ROOT** değeri, tüm öğeler ağaç görünümü denetiminden silinir.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#4](../../mfc/reference/codesnippet/cpp/ctreectrl-class_4.cpp)]  
  
##  <a name="editlabel"></a>  CTreeCtrl::EditLabel  
 Belirtilen öğenin metninin yerinde düzenlemeye başlamak için bu işlevini çağırın.  
  
```  
CEdit* EditLabel(HTREEITEM hItem);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Düzenlenecek ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılı olursa, bir işaretçi `CEdit` öğe metnini düzenlemek için kullanılan; Aksi takdirde nesne **NULL**.  
  
### <a name="remarks"></a>Açıklamalar  
 Düzenleme metnin öğenin metni içeren bir tek satırlı düzenleme denetimi ile değiştirerek gerçekleştirilir.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#5](../../mfc/reference/codesnippet/cpp/ctreectrl-class_5.cpp)]  
  
##  <a name="endeditlabelnow"></a>  CTreeCtrl::EndEditLabelNow  
 Geçerli ağaç görünümü denetimindeki ağaç görünümü öğesinin etiketini düzenleme işlemi sonlanır.  
  
```  
BOOL EndEditLabelNow(BOOL fCancelWithoutSave);
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `fCancelWithoutSave`|`true` düzenleme işlemi tamamlanırken önce ağaç görünümü öğesi yapılan değişiklikleri atmak için veya `false` işlemi tamamlanırken önce ağaç görünümü öğesine değişiklikler kaydedilemiyor.|  
  
### <a name="return-value"></a>Dönüş Değeri  
 `true` Bu yöntem başarılı olursa; Aksi takdirde `false`.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu yöntem gönderir [TVM_ENDEDITLABELNOW](http://msdn.microsoft.com/library/windows/desktop/bb773564) Windows SDK'ın açıklanan ileti.  
  
##  <a name="ensurevisible"></a>  CTreeCtrl::EnsureVisible  
 Ağaç görünümü öğesi görünür olduğundan emin olmak için bu işlevini çağırın.  
  
```  
BOOL EnsureVisible(HTREEITEM hItem);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi görünür yapılan tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Döndürür **TRUE** Sistem belirtilen öğeyi görünür olduğundan emin olmak için ağaç görünümü denetimindeki öğeleri kaydırılan durumunda. Aksi takdirde, dönüş değeri olan **FALSE**.  
  
### <a name="remarks"></a>Açıklamalar  
 Gerekirse, işlevi üst öğeyi genişletir veya öğesi görünür olmasını sağlamak ağaç görünümü denetiminin birlikte kayar.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#6](../../mfc/reference/codesnippet/cpp/ctreectrl-class_6.cpp)]  
  
##  <a name="expand"></a>  CTreeCtrl::Expand  
 İlişkilendirilen tüm, belirtilen üst öğe ile genişletmek veya alt öğeleri listesini daraltmak için bu işlevi çağırır.  
  
```  
BOOL Expand(
    HTREEITEM hItem,  
    UINT nCode);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Genişletilmekte olan ağaç öğesi tanıtıcısı.  
  
 `nCode`  
 Gerçekleştirilecek eylemin türünü belirten bir bayrak. Bu bayrak şu değerlerden biri olabilir:  
  
- `TVE_COLLAPSE` Listenin daraltır.  
  
- `TVE_COLLAPSERESET` Listenin daraltır ve alt öğeleri kaldırır. **TVIS_EXPANDEDONCE** durumu bayrağı sıfırlanır. Bu bayrak ile birlikte kullanılmalıdır `TVE_COLLAPSE` bayrağı.  
  
- `TVE_EXPAND` Listenin genişletir.  
  
- `TVE_TOGGLE` Şu anda genişletilmiş ya da şu anda daraltılmışsa genişletir listesi daraltır.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::EnsureVisible](#ensurevisible).  
  
##  <a name="getbkcolor"></a>  CTreeCtrl::GetBkColor  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_GETBKCOLOR](http://msdn.microsoft.com/library/windows/desktop/bb773570), Windows SDK'ın açıklandığı gibi.  
  
```  
COLORREF GetBkColor() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 A **COLORREF** denetimi için geçerli pencere arka plan rengi temsil eden bir değer. Bu değer -1 ise, Denetim sistem pencere rengi kullanıyor. Bu durumda, kullanabileceğiniz `::GetSysColor(COLOR_WINDOW)` denetimi kullanarak geçerli sistem rengi alınamıyor.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::SetTextColor](#settextcolor).  
  
##  <a name="getcheck"></a>  CTreeCtrl::GetCheck  
 Bir öğenin onay durumunu almak için bu üye işlevini çağırın.  
  
```  
BOOL GetCheck(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 **HTREEITEM** hakkındaki durum bilgilerini almak için.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Ağaç denetim öğesi işaretlenirse sıfır olmayan; Aksi takdirde 0.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::SetCheck](#setcheck).  
  
##  <a name="getchilditem"></a>  CTreeCtrl::GetChildItem  
 Ağaç almak için bu işlevi görüntülemek öğesi, belirtilen öğesinin alt çağrıdır `hItem`.  
  
```  
HTREEITEM GetChildItem(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılı olursa alt öğesi tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#7](../../mfc/reference/codesnippet/cpp/ctreectrl-class_7.cpp)]  
  
##  <a name="getcount"></a>  CTreeCtrl::GetCount  
 Ağaç görünümü denetimindeki öğeleri sayısını almak için bu işlevini çağırın.  
  
```  
UINT GetCount() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Ağaç görünümü denetiminin öğelerin sayısı.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#8](../../mfc/reference/codesnippet/cpp/ctreectrl-class_8.cpp)]  
  
##  <a name="getdrophilightitem"></a>  CTreeCtrl::GetDropHilightItem  
 Sürükle ve bırak işlemi hedefidir öğeyi geri almak için bu işlevini çağırın.  
  
```  
HTREEITEM GetDropHilightItem() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılı olursa bırakılan öğesi tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#9](../../mfc/reference/codesnippet/cpp/ctreectrl-class_9.cpp)]  
  
##  <a name="geteditcontrol"></a>  CTreeCtrl::GetEditControl  
 Ağaç görünümü öğenin metni düzenlemek için kullanılan düzenleme denetimi tanıtıcısı almak için bu işlevini çağırın.  
  
```  
CEdit* GetEditControl() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılı olursa öğe metnini düzenlemek için kullanılan düzenleme denetimine işaretçi; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#10](../../mfc/reference/codesnippet/cpp/ctreectrl-class_10.cpp)]  
  
##  <a name="getextendedstyle"></a>  CTreeCtrl::GetExtendedStyle  
 Geçerli ağaç görünümü denetimi kullanarak genişletilmiş stili alır.  
  
```  
DWORD GetExtendedStyle() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Geçerli ağaç görünümü denetim Bitsel bir birleşimi (veya) içeren bir değer stilleri genişletilmiş. Daha fazla bilgi için bkz: [ağaç görünümü denetimi genişletilmiş stilleri](http://msdn.microsoft.com/library/windows/desktop/bb759981).  
  
### <a name="remarks"></a>Açıklamalar  
 Bu yöntem gönderir [TVM_GETEXTENDEDSTYLE](http://msdn.microsoft.com/library/windows/desktop/bb773580) Windows SDK'ın açıklanan ileti.  
  
##  <a name="getfirstvisibleitem"></a>  CTreeCtrl::GetFirstVisibleItem  
 Ağaç görünümü denetiminin ilk görünür öğesini almak için bu işlevini çağırın.  
  
```  
HTREEITEM GetFirstVisibleItem() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Görünen ilk öğenin tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::SetCheck](#setcheck).  
  
##  <a name="getimagelist"></a>  CTreeCtrl::GetImageList  
 Normal tanıtıcısı veya ağaç görünümü denetimi ile ilişkili durumu görüntü listesini almak için bu işlevini çağırın.  
  
```  
CImageList* GetImageList(UINT nImageList) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `nImageList`  
 Almak için resim listesi türü. Resim listesi aşağıdaki değerlerden biri olabilir:  
  
- `TVSIL_NORMAL` Ağaç görünümü öğesinin seçili ve nonselected görüntüleri içeren normal resim listesi alır.  
  
- `TVSIL_STATE` Kullanıcı tanımlı bir durumda olan ağaç görünümü öğeleri için görüntüleri içeren durumu resim listesi alır.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Denetimin resim listesi başarılı olursa işaretçi; Aksi takdirde **NULL**.  
  
### <a name="remarks"></a>Açıklamalar  
 Her öğe bir ağaç görünümü denetiminin ilişkili eşlemli görüntüleri çifti olabilir. Bir görüntü öğesi seçilir ve öğe seçilmediğinde diğer görüntülendiğinde görüntülenir. Örneğin, değil seçildiğinde bir öğe seçildiğinde açık bir klasörde ve kapalı klasörü görüntülenebilir.  
  
 Görüntü listeleri hakkında daha fazla bilgi için bkz: [Cımagelist](../../mfc/reference/cimagelist-class.md) sınıfı.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#11](../../mfc/reference/codesnippet/cpp/ctreectrl-class_11.cpp)]  
  
##  <a name="getindent"></a>  CTreeCtrl::GetIndent  
 Tutar, piksel cinsinden o alt öğeleri kendi üst öğeler göre girintili almak için bu işlevini çağırın.  
  
```  
UINT GetIndent() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Girinti miktarını piksel cinsinden ölçülür.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#12](../../mfc/reference/codesnippet/cpp/ctreectrl-class_12.cpp)]  
  
##  <a name="getinsertmarkcolor"></a>  CTreeCtrl::GetInsertMarkColor  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_GETINSERTMARKCOLOR](http://msdn.microsoft.com/library/windows/desktop/bb773590), Windows SDK'ın açıklandığı gibi.  
  
```  
COLORREF GetInsertMarkColor() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 A **COLORREF** değeri geçerli ekleme işareti renk içerir.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#13](../../mfc/reference/codesnippet/cpp/ctreectrl-class_13.cpp)]  
  
##  <a name="getitem"></a>  CTreeCtrl::GetItem  
 Belirtilen ağaç görünümü öğesi özniteliklerini almak için bu işlevini çağırın.  
  
```  
BOOL GetItem(TVITEM* pItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `pItem`  
 Bir işaretçi bir [TVITEM](http://msdn.microsoft.com/library/windows/desktop/bb773456) , Windows SDK'ın açıklandığı gibi yapılandırın.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::DeleteItem](#deleteitem).  
  
##  <a name="getitemdata"></a>  CTreeCtrl::GetItemData  
 Belirtilen öğeyle ilişkili 32-bit uygulamaya özgü değerini almak için bu işlevini çağırın.  
  
```  
DWORD_PTR GetItemData(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Alınacak verileri olan öğesinin tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Belirtilen öğe ilişkilendirilen bir 32 bit uygulamaya özgü değer `hItem`.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#14](../../mfc/reference/codesnippet/cpp/ctreectrl-class_14.cpp)]  
  
##  <a name="getitemexpandedimageindex"></a>  CTreeCtrl::GetItemExpandedImageIndex  
 Belirtilen öğe geçerli ağaç görünümü denetiminin genişletilmiş durumdayken görüntülenecek resim dizinini alır.  
  
```  
int GetItemExpandedImageIndex(HTREEITEM hItem)const;  
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `hItem`|Bir ağaç görünümü denetimi öğesi işler.|  
  
### <a name="return-value"></a>Dönüş Değeri  
 Belirtilen öğe genişletilmiş durumdayken görüntülenecek resim dizini.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu yöntem gönderir [TVM_GETITEM](http://msdn.microsoft.com/library/windows/desktop/bb773596) Windows SDK'ın açıklanan ileti. Döndürür ileti [TVITEMEX](http://msdn.microsoft.com/library/windows/desktop/bb773459) ağaç görünümü denetim öğesi ve bu yöntem açıklayan yapısı alır `iExpandedImage` yapıyı üyeden.  
  
##  <a name="getitemheight"></a>  CTreeCtrl::GetItemHeight  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_GETITEMHEIGHT](http://msdn.microsoft.com/library/windows/desktop/bb773599), Windows SDK'ın açıklandığı gibi.  
  
```  
SHORT GetItemHeight() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Öğesinin piksel cinsinden yüksekliği.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#15](../../mfc/reference/codesnippet/cpp/ctreectrl-class_15.cpp)]  
  
##  <a name="getitemimage"></a>  CTreeCtrl::GetItemImage  
 Her öğe bir ağaç görünümü denetiminin ilişkili eşlemli görüntüleri çifti olabilir.  
  
```  
BOOL GetItemImage(
    HTREEITEM hItem,  
    int& nImage,  
    int& nSelectedImage) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Alınacak olan görüntüdür öğesinin tanıtıcısı.  
  
 `nImage`  
 Ağaç görünümü denetimin resim listesi içindeki öğesi'nin görüntüsünün dizinini alır bir tamsayı.  
  
 `nSelectedImage`  
 Ağaç görünümü denetimin resim listesi içinde seçilen görüntü öğenin dizinini alır bir tamsayı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 Görüntüleri öğenin etiketi sol tarafında görünür. Bir görüntü öğesi seçilir ve öğe seçilmediğinde diğer görüntülendiğinde görüntülenir. Örneğin, değil seçildiğinde bir öğe seçildiğinde açık bir klasörde ve kapalı klasörü görüntülenebilir.  
  
 Öğenin resmi ve ağaç görünümü denetimin resim listesi içinde seçilen görüntü dizini almak için bu işlevini çağırın.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#16](../../mfc/reference/codesnippet/cpp/ctreectrl-class_16.cpp)]  
  
##  <a name="getitempartrect"></a>  CTreeCtrl::GetItemPartRect  
 Geçerli ağaç görünümü Denetim belirtilen öğesinde belirtilen bir kısmı için sınırlayıcı dikdörtgenini alır.  
  
```  
BOOL GetItemPartRect(
    HTREEITEM hItem,   
    int nPart,   
    LPRECT lpRect)const;  
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `hItem`|Bir ağaç görünümü denetimi öğesi işler.|  
|[in] `nPart`|Bölümü için tanımlayıcı. ayarlanmalıdır `TVGIPR_BUTTON`.|  
|[out] `lpRect`|İşaretçi bir [RECT](http://msdn.microsoft.com/library/windows/desktop/dd162897) yapısı. Bu yöntem başarılı olursa, yapısı tarafından belirtilen bölümü dikdörtgen koordinatlarını alır `hItem` ve `nPart`.|  
  
### <a name="return-value"></a>Dönüş Değeri  
 `true` Bu yöntem başarılı olursa; Aksi takdirde `false`.  
  
### <a name="remarks"></a>Açıklamalar  
 Her ağaç denetim öğesi tarafından grafik dikdörtgen sınırlıdır. Her Bu dikdörtgeni noktasında tıklatıldığında öğesi olarak kabul edilir *isabet*. Dikdörtgen noktasında tıklandığında öğesi tarafından tanımlanan şekilde bu yöntem en büyük dikdörtgen döndürür `hItem` parametre isabet.  
  
 Bu yöntem gönderir `TVM_GETITEMPARTRECT` Windows SDK'ın açıklanan ileti. Daha fazla bilgi için bkz: [TreeView_GetItemPartRect](http://msdn.microsoft.com/library/windows/desktop/bb773847) makrosu.  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneği, bir değişken tanımlar `m_treeCtrl`, yani geçerli ağaç görünümü denetim erişmek için kullanılır. Kod örneği, imzasız tamsayı ve birkaç HTREEITEM değişkenleri de tanımlar. Bu değişkenler sonraki örnekte kullanılır.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneğinde erişilebilirlik tanımlayıcı kullanır ve [CTreeCtrl::MapAccIdToItem](#mapaccidtoitem) kök ağaç görünümü öğesi için bir tanıtıcı alma yöntemi. Örnek işleyicisini kullanıyorsa ve [CTreeCtrl::GetItemPartRect](#getitempartrect) öğenin etrafında 3B dikdörtgen çizmek için yöntem. Bir gösterilmez, kod örneği, önceki bölümde bir ağaç oluşan Amerika Birleşik Devletleri için bir kök ülke/bölge düğümü, alt düğümleri Pennsylvania ve Washington durumları için ve bu durumlar şehirlerde öğeleri ağacı görünümü oluşturduk. Kullandık [CTreeCtrl::MapItemToAccID](#mapitemtoaccid) kök ağaç görünümü öğesi erişilebilirlik tanımlayıcısı ile ilişkilendirilecek yöntemi.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#5](../../mfc/reference/codesnippet/cpp/ctreectrl-class_18.cpp)]  
  
##  <a name="getitemrect"></a>  CTreeCtrl::GetItemRect  
 İçin sınırlayıcı dikdörtgenini almak için bu işlevi çağırmak `hItem` ve onu görünür olup olmadığını belirler.  
  
```  
BOOL GetItemRect(
    HTREEITEM hItem,  
    LPRECT lpRect,  
    BOOL bTextOnly) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç görünümü denetim öğesinin tanıtıcısı.  
  
 `lpRect`  
 İşaretçi bir [RECT](http://msdn.microsoft.com/library/windows/desktop/dd162897) sınırlayıcı dikdörtgenini alır yapısı. Koordinatlar ağaç görünümü denetimi sol üst köşesindeki göre belirlenir.  
  
 *bTextOnly*  
 Bu parametre sıfır değilse, sınırlayıcı dikdörtgenini yalnızca öğenin metni içerir. Aksi takdirde ağaç görünümü denetiminde öğe kapladığı tüm satır içerir.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Öğe içinde yer alan sınırlayıcı dikdörtgenini ile görünmüyorsa, sıfır olmayan `lpRect`. Aksi takdirde, 0 ile `lpRect` başlatılmamış.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#17](../../mfc/reference/codesnippet/cpp/ctreectrl-class_19.cpp)]  
  
##  <a name="getitemstate"></a>  CTreeCtrl::GetItemState  
 Belirtilen öğenin durumunu verir `hItem`.  
  
```  
UINT GetItemState(
    HTREEITEM hItem,  
    UINT nStateMask) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Alınacak durumu olan öğesinin tanıtıcısı.  
  
 `nStateMask`  
 Alınacak bir veya daha fazla durumları gösteren maskesi. İçin olası değerler hakkında daha fazla bilgi için `nStateMask`, açıklamalara **durumu** ve **stateMask** üyeleri [TVITEM](http://msdn.microsoft.com/library/windows/desktop/bb773456) Windows SDK'sındaki yapısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 A **UINT** nStateMask tarafından belirtilen değerlerin Bitsel veya tutar. Olası değerler hakkında daha fazla bilgi için bkz: [CTreeCtrl::GetItem](#getitem). İçin belirli bir durumda değeri bulmak için aşağıdaki örnekte gösterildiği gibi durum değeri ve dönüş değeri bit tabanlı AND işlemi gerçekleştirin.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#18](../../mfc/reference/codesnippet/cpp/ctreectrl-class_20.cpp)]  
  
##  <a name="getitemstateex"></a>  CTreeCtrl::GetItemStateEx  
 Belirtilen öğe geçerli ağaç görünümü denetiminde genişletilmiş durumunu alır.  
  
```  
UINT GetItemStateEx(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `hItem`|Bir ağaç görünümü denetimi öğesi işler.|  
  
### <a name="return-value"></a>Dönüş Değeri  
 Öğesinin genişletilmiş durumu. Daha fazla bilgi için bkz: `uStateEx` üyesi [TVITEMEX](http://msdn.microsoft.com/library/windows/desktop/bb773459) yapısı.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu yöntem gönderir [TVM_GETITEM](http://msdn.microsoft.com/library/windows/desktop/bb773596) Windows SDK'ın açıklanan ileti. Döndürür ileti [TVITEMEX](http://msdn.microsoft.com/library/windows/desktop/bb773459) ağaç görünümü denetim öğesi ve bu yöntem açıklayan yapısı alır `uStateEx` yapıyı üyeden.  
  
##  <a name="getitemtext"></a>  CTreeCtrl::GetItemText  
 Belirtilen öğenin metnini döndürür `hItem`.  
  
```  
CString GetItemText(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Alınacak metni olan öğesinin tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 A `CString` öğenin metni içeren nesne.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::GetNextItem](#getnextitem).  
  
##  <a name="getlastvisibleitem"></a>  CTreeCtrl::GetLastVisibleItem  
 Geçerli ağaç görünümü denetiminde son unexpanded düğüm öğesini alır.  
  
```  
HTREEITEM GetLastVisibleItem() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Yöntem başarılı olursa son unexpanded düğüm öğesini tanıtıcısını; Aksi takdirde `NULL`.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu yöntem gönderir [TVM_GETNEXTITEM](http://msdn.microsoft.com/library/windows/desktop/bb773622) Windows SDK'ın açıklanan ileti. Daha fazla bilgi için bkz: `TVGN_LASTVISIBLE` içinde bayrak `flag` söz konusu iletinin parametresi.  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneği, bir değişken tanımlar `m_treeCtrl`, yani geçerli ağaç görünümü denetim erişmek için kullanılır. Kod örneği, imzasız tamsayı ve birkaç HTREEITEM değişkenleri de tanımlar. Sonraki örnekte, bir veya daha fazla bu değişkenleri kullanılır.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneğinde son unexpanded ağaç görünümü düğümü öğesi için bir tanıtıcı alır ve bu öğe etrafında 3B dikdörtgen çizer. Bir gösterilmez, kod örneği, önceki bölümde bir ağaç oluşan Amerika Birleşik Devletleri için bir kök ülke/bölge düğümü, alt düğümleri Pennsylvania ve Washington durumları için ve bu durumlar şehirlerde öğeleri ağacı görünümü oluşturduk.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#6](../../mfc/reference/codesnippet/cpp/ctreectrl-class_21.cpp)]  
  
##  <a name="getlinecolor"></a>  CTreeCtrl::GetLineColor  
 Bu üye işlevi win32 ileti davranışını uygulayan [TVM_GETLINECOLOR](http://msdn.microsoft.com/library/windows/desktop/bb773619), Windows SDK'ın açıklandığı gibi.  
  
```  
COLORREF GetLineColor() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Geçerli çizgi rengi.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#19](../../mfc/reference/codesnippet/cpp/ctreectrl-class_22.cpp)]  
  
##  <a name="getnextitem"></a>  CTreeCtrl::GetNextItem  
 Çağrı ağacı almak için bu işlevi görünüm öğesi olduğundan sahip belirttiği belirtilen ilişki `nCode` parametresi, `hItem`.  
  
```  
HTREEITEM GetNextItem(
    HTREEITEM hItem,  
    UINT nCode) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
 `nCode`  
 İlişki türünü belirten bir bayrak `hItem`. Bu bayrak aşağıdaki değerlerden biri olabilir:  
  
- `TVGN_CARET` Seçili öğeyi alır.  
  
- `TVGN_CHILD` Belirtilen öğenin ilk alt öğesi alır `hItem` parametresi.  
  
- `TVGN_DROPHILITE` Sürükle ve bırak işlemi hedefidir öğesini alır.  
  
- `TVGN_FIRSTVISIBLE` İlk görünür öğeyi alır.  
  
- `TVGN_LASTVISIBLE` Ağacında son genişletilmiş öğeyi alır. Bu, ağaç görünümünde penceresinde görünür son öğeyi almaz.  
  
- `TVGN_NEXT` Sonraki eşdüzey öğeyi alır.  
  
- `TVGN_NEXTVISIBLE` Belirtilen öğe izleyen sonraki görünür öğeyi alır.  
  
- `TVGN_PARENT` Belirtilen öğenin üst alır.  
  
- `TVGN_PREVIOUS` Önceki eşdüzey öğeyi alır.  
  
- `TVGN_PREVIOUSVISIBLE` Belirtilen öğe önündeki ilk görünür öğeyi alır.  
  
- `TVGN_ROOT` Belirtilen öğe bir parçası olduğu kök öğesinin ilk alt öğesi alır.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılı olursa sonraki öğe tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu işlev döndürülecek **NULL** alınan öğe ağacı kök düğümü ise. Örneğin, bu iletiyle kullanırsanız `TVGN_PARENT` bayrak bir birinci düzey alt ağaç görünümün kök düğümünün iletisi verecektir **NULL**.  
  
### <a name="example"></a>Örnek  
 Kullanarak bir örnek için `GetNextItem` bir döngüde bkz [CTreeCtrl::DeleteItem](#deleteitem).  
  
 [!code-cpp[NVC_MFC_CTreeCtrl#20](../../mfc/reference/codesnippet/cpp/ctreectrl-class_23.cpp)]  
  
##  <a name="getnextsiblingitem"></a>  CTreeCtrl::GetNextSiblingItem  
 Sonraki eşdüzey almak için bu işlevi çağırmak `hItem`.  
  
```  
HTREEITEM GetNextSiblingItem(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Sonraki eşdüzey öğeyi tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#21](../../mfc/reference/codesnippet/cpp/ctreectrl-class_24.cpp)]  
  
##  <a name="getnextvisibleitem"></a>  CTreeCtrl::GetNextVisibleItem  
 Sonraki görünür öğesi'nin almak için bu işlevi çağırmak `hItem`.  
  
```  
HTREEITEM GetNextVisibleItem(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Sonraki görünür öğeyi tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::SetCheck](#setcheck).  
  
##  <a name="getparentitem"></a>  CTreeCtrl::GetParentItem  
 Üst almak için bu işlevi çağırmak `hItem`.  
  
```  
HTREEITEM GetParentItem(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Üst öğesi tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu işlev döndürülecek **NULL** belirtilen öğenin üst ağacı kök düğümü ise.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::EnsureVisible](#ensurevisible).  
  
##  <a name="getprevsiblingitem"></a>  CTreeCtrl::GetPrevSiblingItem  
 Önceki eşdüzey almak için bu işlevi çağırmak `hItem`.  
  
```  
HTREEITEM GetPrevSiblingItem(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Önceki eşdüzey tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#22](../../mfc/reference/codesnippet/cpp/ctreectrl-class_25.cpp)]  
  
##  <a name="getprevvisibleitem"></a>  CTreeCtrl::GetPrevVisibleItem  
 Önceki görünür öğesi'nin almak için bu işlevi çağırmak `hItem`.  
  
```  
HTREEITEM GetPrevVisibleItem(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Önceki görünür öğeyi tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#23](../../mfc/reference/codesnippet/cpp/ctreectrl-class_26.cpp)]  
  
##  <a name="getrootitem"></a>  CTreeCtrl::GetRootItem  
 Ağaç görünümü denetiminin kök öğesinin almak için bu işlevini çağırın.  
  
```  
HTREEITEM GetRootItem() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Kök öğesinin tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::EditLabel](#editlabel).  
  
##  <a name="getscrolltime"></a>  CTreeCtrl::GetScrollTime  
 Ağaç görünümü denetimi için en fazla kaydırma süresi almak için bu üye işlevini çağırın.  
  
```  
UINT GetScrollTime() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 En fazla kaydırma milisaniye cinsinden süre.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu üye işlevi win32 ileti davranışını uygulayan [TVM_GETSCROLLTIME](http://msdn.microsoft.com/library/windows/desktop/bb773625), Windows SDK'ın açıklandığı gibi.  
  
##  <a name="getselectedcount"></a>  CTreeCtrl::GetSelectedCount  
 Geçerli ağaç görünümü denetimindeki seçili öğe sayısını alır.  
  
```  
UINT GetSelectedCount();
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Seçili öğe sayısı.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu yöntem gönderir [TVM_GETSELECTEDCOUNT](http://msdn.microsoft.com/library/windows/desktop/bb773629) Windows SDK'ın açıklanan ileti.  
  
##  <a name="getselecteditem"></a>  CTreeCtrl::GetSelectedItem  
 Seçili öğenin ağaç görünümü denetiminin almak için bu işlevini çağırın.  
  
```  
HTREEITEM GetSelectedItem() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Seçili öğeyi tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#24](../../mfc/reference/codesnippet/cpp/ctreectrl-class_27.cpp)]  
  
##  <a name="gettextcolor"></a>  CTreeCtrl::GetTextColor  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_GETTEXTCOLOR](http://msdn.microsoft.com/library/windows/desktop/bb773633), Windows SDK'ın açıklandığı gibi.  
  
```  
COLORREF GetTextColor() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 A **COLORREF** geçerli metin rengini gösteren bir değer. Bu değer -1 ise, Denetim metin rengini sistem rengi kullanıyor.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::SetTextColor](#settextcolor).  
  
##  <a name="gettooltips"></a>  CTreeCtrl::GetToolTips  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_GETTOOLTIPS](http://msdn.microsoft.com/library/windows/desktop/bb773729), Windows SDK'ın açıklandığı gibi.  
  
```  
CToolTipCtrl* GetToolTips() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Bir işaretçi bir [CToolTipCtrl](../../mfc/reference/ctooltipctrl-class.md) ağaç denetimi tarafından kullanılan nesne. Varsa [oluşturma](#create) üye işlevini kullanır stili **TVS_NOTOOLTIPS**, hiçbir araç ipuçları kullanılan ve **NULL** döndürülür.  
  
### <a name="remarks"></a>Açıklamalar  
 MFC uygulaması `GetToolTips` döndüren bir `CToolTipCtrl` bir araç ipucu denetimi için bir tanıtıcı yerine ağaç denetimi tarafından kullanılan nesne.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#25](../../mfc/reference/codesnippet/cpp/ctreectrl-class_28.cpp)]  
  
##  <a name="getvisiblecount"></a>  CTreeCtrl::GetVisibleCount  
 Ağaç görünümü denetimi görünür öğeleri sayısını almak için bu işlevini çağırın.  
  
```  
UINT GetVisibleCount() const;  
```  
  
### <a name="return-value"></a>Dönüş Değeri  
 Ağaç görünümü denetiminin görünür öğelerin sayısı; Aksi takdirde - 1.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::SetCheck](#setcheck).  
  
##  <a name="hittest"></a>  CTreeCtrl::HitTest  
 Ağaç görünümü denetimi belirtilen noktası istemci alanını göreli konumunu belirlemek için bu işlevini çağırın.  
  
```  
HTREEITEM HitTest(
    CPoint pt,  
    UINT* pFlags = NULL) const;  
  
HTREEITEM HitTest(TVHITTESTINFO* pHitTestInfo) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `pt`  
 Test etmek için istemci koordinatları noktası.  
  
 `pFlags`  
 İşaretçi tamsayıya isabet testi sonuçlarıyla ilgili bilgileri alır. Bir olabilir ya da daha fazla değeri altında listelenen **bayrakları** açıklamalar bölümünde üye.  
  
 `pHitTestInfo`  
 Adres bir [TVHITTESTINFO](http://msdn.microsoft.com/library/windows/desktop/bb773448) test ve, isabet konumuna içeren yapısı isabet testi sonuçlarıyla ilgili bilgileri alır.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Belirtilen nokta kapladığı ağaç görünümü öğesi tanıtıcısı veya **NULL** öğe noktası kapladığı durumunda.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu işlev çağrıldığında `pt` parametresi, test etmek için noktası koordinatları belirtir. İşlev öğesi belirtilen noktada işleyicisini döndürür veya **NULL** öğe noktası kapladığı durumunda. Ayrıca, `pFlags` parametresi belirtilen noktasının konumunu belirten bir değer içeriyor. Olası değerler şunlardır:  
  
|||  
|-|-|  
|Değer|Açıklama|  
|TVHT_ABOVE|İstemci alanı üzerinde.|  
|TVHT_BELOW|İstemci alanının altında.|  
|TVHT_NOWHERE|İstemci alanını, ancak son öğenin altına.|  
|TVHT_ONITEM|Bit eşlem veya bir öğesiyle ilişkilendirilmiş etiket.|  
|TVHT_ONITEMBUTTON|Düğmeyi bir öğesiyle ilişkilendirilmiş.|  
|TVHT_ONITEMICON|Bir öğesiyle ilişkili bitmap üzerinde.|  
|TVHT_ONITEMINDENT|Girinti bir öğesiyle ilişkilendirilmiş.|  
|TVHT_ONITEMLABEL|Bir öğesiyle ilişkili etiketi (dize).|  
|TVHT_ONITEMRIGHT|Bir öğenin sağına alanında.|  
|TVHT_ONITEMSTATEICON|Ağaç görünümü öğesi için durum simgesinde, kullanıcı tanımlı bir durumda değil.|  
|TVHT_TOLEFT|İstemci alanını soluna.|  
|TVHT_TORIGHT|İstemci alanını sağında.|  
|||  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#26](../../mfc/reference/codesnippet/cpp/ctreectrl-class_29.cpp)]  
  
##  <a name="insertitem"></a>  CTreeCtrl::InsertItem  
 Ağaç görünümü denetiminde yeni bir öğe eklemek için bu işlevini çağırın.  
  
```  
HTREEITEM InsertItem(LPTVINSERTSTRUCT lpInsertStruct);

 
HTREEITEM InsertItem(
    UINT nMask,  
    LPCTSTR lpszItem,  
    int nImage,  
    int nSelectedImage,  
    UINT nState,  
    UINT nStateMask,  
    LPARAM lParam,  
    HTREEITEM hParent,  
    HTREEITEM hInsertAfter);

 
HTREEITEM InsertItem(
    LPCTSTR lpszItem,  
    HTREEITEM hParent = TVI_ROOT,  
    HTREEITEM hInsertAfter = TVI_LAST);

 
HTREEITEM InsertItem(
    LPCTSTR lpszItem,  
    int nImage,  
    int nSelectedImage,  
    HTREEITEM hParent = TVI_ROOT,  
    HTREEITEM hInsertAfter = TVI_LAST);
```  
  
### <a name="parameters"></a>Parametreler  
 *lpInsertStruct*  
 Bir işaretçi bir `TVINSERTSTRUCT` eklenecek ağaç görünümü öğesi özniteliklerini belirtir.  
  
 `nMask`  
 Ayarlamak için hangi özniteliklerin belirten tamsayı. Bkz: `TVITEM` Windows SDK'sındaki yapısı.  
  
 `lpszItem`  
 Öğenin metni içeren bir dize adresidir.  
  
 `nImage`  
 Ağaç görünümü denetimin görüntü listesinde öğesi'nin görüntü dizini.  
  
 `nSelectedImage`  
 Ağaç görünümü denetimin görüntü listesinde seçilen görüntü öğenin dizini.  
  
 `nState`  
 Öğenin durumlar için değerler belirtir. Windows SDK'sındaki uygun durumların listesi için bkz: ağaç görünümü denetim öğesi durumlarına.  
  
 `nStateMask`  
 Hangi durumları için ayarlanacak belirtir. Bkz: `TVITEM` Windows SDK'sındaki yapısı.  
  
 `lParam`  
 Bu öğeyle ilişkili 32-bit uygulamaya özgü bir değer.  
  
 `hParent`  
 Eklenen öğenin üst tanıtıcısı.  
  
 *hInsertAfter*  
 Sonra yeni öğe eklenecek madde tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılı olursa yeni öğe tanıtıcısı; Aksi takdirde **NULL**.  
  
### <a name="remarks"></a>Açıklamalar  
 Örnek bir ağaç denetim öğesi eklerken her işlevi sürümünü kullanmak isteyebileceğiniz durumlara gösterir.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#27](../../mfc/reference/codesnippet/cpp/ctreectrl-class_30.cpp)]  
  
##  <a name="itemhaschildren"></a>  CTreeCtrl::ItemHasChildren  
 Ağaç öğesi tarafından belirtilen olup olmadığını belirlemek için bu işlevi kullanın `hItem` alt öğeleri vardır.  
  
```  
BOOL ItemHasChildren(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Ağaç öğesi tarafından belirtilen sıfır olmayan `hItem` alt öğeleri; yoksa 0.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu nedenle, daha sonra kullanabilirsiniz, [CTreeCtrl::GetChildItem](#getchilditem) bu alt öğeleri alınamadı.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::GetSelectedItem](#getselecteditem).  
  
##  <a name="mapaccidtoitem"></a>  CTreeCtrl::MapAccIdToItem  
 Geçerli ağaç görünümü denetimindeki ağaç görünümü öğesinin işlenecek belirtilen erişilebilirlik tanımlayıcı eşler.  
  
```  
HTREEITEM MapAccIdToItem(UINT uAccId) const;  
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `uAccId`|Ağaç görünümü öğesi bir öğe için erişilebilirlik tanımlayıcısı.|  
  
### <a name="return-value"></a>Dönüş Değeri  
 Ağaç görünümü öğesi tanıtıcısını ( `HTREEITEM`) karşılık gelen `uAccId` parametresi. Daha fazla bilgi için bkz: `hItem` üyesi [TVITEMEX](http://msdn.microsoft.com/library/windows/desktop/bb773459) yapısı.  
  
### <a name="remarks"></a>Açıklamalar  
 Erişilebilirlik yardımları engelli kişilere yardımcı uygulamalarını bilgisayarları kullanılır. Tarafından kullanılan bir erişilebilirlik tanımlayıcısı `IAccessible` benzersiz şekilde bir öğeyi bir pencerede belirtmek için arabirim. "Hakkında Active Accessibility desteği" konusuna erişilebilirlik tanımlayıcıları hakkında daha fazla bilgi için arama [Microsoft Developer Network](http://go.microsoft.com/fwlink/p/?linkid=56322).  
  
 Bu yöntem gönderir [TVM_MAPACCIDTOHTREEITEM](http://msdn.microsoft.com/library/windows/desktop/bb773734) Windows SDK'ın açıklanan ileti.  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneği, bir değişken tanımlar `m_treeCtrl`, yani geçerli ağaç görünümü denetim erişmek için kullanılır. Kod örneği, imzasız tamsayı ve birkaç HTREEITEM değişkenleri de tanımlar. Bu değişkenler sonraki örnekte kullanılır.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneğinde erişilebilirlik tanımlayıcı kullanır ve [CTreeCtrl::MapAccIdToItem](#mapaccidtoitem) kök ağaç görünümü öğesi için bir tanıtıcı alma yöntemi. Örnek tanıtıcı kullanır ve [CTreeCtrl::GetItemPartRect](#getitempartrect) öğenin etrafında 3B dikdörtgen çizmek için yöntem. Bir gösterilmez, kod örneği, önceki bölümde bir ağaç oluşan Amerika Birleşik Devletleri için bir kök ülke/bölge düğümü, alt düğümleri Pennsylvania ve Washington durumları için ve bu durumlar şehirlerde öğeleri ağacı görünümü oluşturduk. Kullandık [CTreeCtrl::MapItemToAccID](#mapitemtoaccid) kök ağaç görünümü öğesi erişilebilirlik tanımlayıcısı ile ilişkilendirilecek yöntemi.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#5](../../mfc/reference/codesnippet/cpp/ctreectrl-class_18.cpp)]  
  
##  <a name="mapitemtoaccid"></a>  CTreeCtrl::MapItemToAccID  
 Geçerli ağaç görünümü denetimindeki ağaç görünümü öğesinin belirtilen tanıtıcı erişilebilirlik tanımlayıcıyı eşler.  
  
```  
UINT MapItemToAccID(HTREEITEM hItem) const;  
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `hItem`|Ağaç görünümü öğesi denetiminde tanıtıcısı. Daha fazla bilgi için bkz: `hItem` üyesi [TVITEMEX](http://msdn.microsoft.com/library/windows/desktop/bb773459) yapısı.|  
  
### <a name="return-value"></a>Dönüş Değeri  
 Karşılık gelen erişilebilirlik tanımlayıcı `hItem` parametresi.  
  
### <a name="remarks"></a>Açıklamalar  
 Erişilebilirlik yardımları engelli kişilere yardımcı uygulamalarını bilgisayarları kullanılır. Tarafından kullanılan bir erişilebilirlik tanımlayıcısı `IAccessible` benzersiz şekilde bir öğeyi bir pencerede belirtmek için arabirim. "Hakkında Active Accessibility desteği" konusuna erişilebilirlik tanımlayıcıları hakkında daha fazla bilgi için arama [Microsoft Developer Network](http://go.microsoft.com/fwlink/p/?linkid=56322).  
  
 Bu yöntem gönderir [TVM_MAPHTREEITEMTOACCID](http://msdn.microsoft.com/library/windows/desktop/bb773735) Windows SDK'ın açıklanan ileti.  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneği, bir değişken tanımlar `m_treeCtrl`, yani geçerli ağaç görünümü denetim erişmek için kullanılır. Kod örneği, imzasız tamsayı ve birkaç HTREEITEM değişkenleri de tanımlar. Bu değişkenler sonraki örnekte kullanılır.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneğinde bir ağaç görünümü denetimi öğesi için bir kimlik numarası alır. Bir gösterilmez, kod örneği, önceki bölümde bir ağaç oluşan Amerika Birleşik Devletleri için bir kök ülke/bölge düğümü, alt düğümleri Pennsylvania ve Washington durumları için ve bu durumlar şehirlerde öğeleri ağacı görünümü oluşturduk. Bu kod örneği kök ülke/bölge düğüm için benzersiz bir kimlik numarası alır.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#2](../../mfc/reference/codesnippet/cpp/ctreectrl-class_31.cpp)]  
  
##  <a name="select"></a>  CTreeCtrl::Select  
 Belirtilen ağaç görünümü öğesini seçin, görünüme öğesi kaydırın veya sürükle ve bırak işlemi hedefinin göstermek için kullanılan stilde öğe yeniden boyutlandırmaya bu işlevini çağırın.  
  
```  
BOOL Select(
    HTREEITEM hItem,  
    UINT nCode);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
 `nCode`  
 Yapılacak eylem türü. Bu parametre aşağıdaki değerlerden biri olabilir:  
  
- `TVGN_CARET` Verilen öğe seçimi ayarlar.  
  
- `TVGN_DROPHILITE` Verilen öğe sürükle ve bırak işlemi hedefinin göstermek için kullanılan stilde yeniden çizer.  
  
- `TVGN_FIRSTVISIBLE` Ağaç görünümü, böylece verilen öğe görünen ilk öğenin dikey olarak kayar.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 Varsa `nCode` değeri içeren `TVGN_CARET`, üst pencere alır **TVN_SELCHANGING** ve **TVN_SELCHANGED** bildirim iletileri. Ayrıca, belirtilen öğeyi daraltılmış üst öğenin alt ise, üst öğenin alt öğeleri listesi belirtilen öğeyi ortaya genişletilir. Bu durumda, ana pencereyi alan **TVN_ITEMEXPANDING** ve **TVN_ITEMEXPANDED** bildirim iletileri.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::HitTest](#hittest).  
  
##  <a name="selectdroptarget"></a>  CTreeCtrl::SelectDropTarget  
 Sürükle ve bırak işlemi hedefinin göstermek için kullanılan stilde öğe yeniden çizmek için bu işlevini çağırın.  
  
```  
BOOL SelectDropTarget(HTREEITEM hItem);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#9](../../mfc/reference/codesnippet/cpp/ctreectrl-class_9.cpp)]  
  
##  <a name="selectitem"></a>  CTreeCtrl::SelectItem  
 Belirtilen ağaç görünümü öğeyi seçmek için bu işlevini çağırın.  
  
```  
BOOL SelectItem(HTREEITEM hItem);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 Varsa `hItem` olan **NULL**, sonra da bu işlev öğe seçer.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#26](../../mfc/reference/codesnippet/cpp/ctreectrl-class_29.cpp)]  
  
##  <a name="selectsetfirstvisible"></a>  CTreeCtrl::SelectSetFirstVisible  
 Böylece verilen öğe görünen ilk öğenin ağaç görünümü dikey olarak kaydırmak için bu işlevini çağırın.  
  
```  
BOOL SelectSetFirstVisible(HTREEITEM hItem);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ağaç öğesi ilk görünür öğesi olarak ayarlanacak tanıtıcısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 İşlev penceresiyle bir ileti gönderir `TVM_SELECTITEM` ve `TVGN_FIRSTVISIBLE` ileti parametreleri.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#28](../../mfc/reference/codesnippet/cpp/ctreectrl-class_32.cpp)]  
  
##  <a name="setautoscrollinfo"></a>  CTreeCtrl::SetAutoscrollInfo  
 Geçerli ağaç görünümü denetim otomatik kaydırma oranını ayarlar.  
  
```  
BOOL SetAutoscrollInfo(
    UINT uPixelsPerSec,   
    UINT uUpdateTime);
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `uPixelsPerSec`|Piksel kaydırmak için saniye başına sayısı.|  
|[in] `uUpdateTime`|Denetim güncelleştirmeleri arasındaki zaman aralığı.|  
  
### <a name="return-value"></a>Dönüş Değeri  
 Her zaman döndürür `true`.  
  
### <a name="remarks"></a>Açıklamalar  
 Otomatik kaydırma parametreleri şu anda görünür olmayan bir öğe görünüme kaydırmak için kullanılır. Ağaç görünümü denetimi olmalıdır `TVS_EX_AUTOHSCROLL` genişletilmiş açıklanan stili [ağaç görünümü denetimi genişletilmiş stilleri](http://msdn.microsoft.com/library/windows/desktop/bb759981).  
  
 Bu yöntem gönderir [TVM_SETAUTOSCROLLINFO](http://msdn.microsoft.com/library/windows/desktop/bb773738) Windows SDK'ın açıklanan ileti.  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneği, bir değişken tanımlar `m_treeCtrl`, yani geçerli ağaç görünümü denetim erişmek için kullanılır. Kod örneği, imzasız tamsayı ve birkaç HTREEITEM değişkenleri de tanımlar. Bu değişkenler sonraki örnekte kullanılır.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneğinde geçerli ağaç görünümü denetiminin otomatik kaydırma davranışını ayarlar. Bir gösterilmez, kod örneği, önceki bölümde bir ağaç oluşan Amerika Birleşik Devletleri için bir kök ülke/bölge düğümü, alt düğümleri Pennsylvania ve Washington durumları için ve bu durumlar şehirlerde öğeleri ağacı görünümü oluşturduk. Otomatik odaklanmış ağaç öğesi görüntülemek için Kaydır gerekir böylece biz bilerek ağaç görünümü denetim dar yapılan. Kod örneği ağaç görünümünde öğesidir kadar her 5 saniyede saniyede 30 piksel otomatik olarak kaydırmak için ağaç görünümü denetimi ayarlar.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#4](../../mfc/reference/codesnippet/cpp/ctreectrl-class_33.cpp)]  
  
##  <a name="setbkcolor"></a>  CTreeCtrl::SetBkColor  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_SETBKCOLOR](http://msdn.microsoft.com/library/windows/desktop/bb773741), Windows SDK'ın açıklandığı gibi.  
  
```  
COLORREF SetBkColor(COLORREF clr);
```  
  
### <a name="parameters"></a>Parametreler  
 `clr`  
 A **COLORREF** yeni arka plan rengini içeren değer. Bu değer -1 ise, denetim arka plan rengini sistem rengi kullanmaya döner.  
  
### <a name="return-value"></a>Dönüş Değeri  
 A **COLORREF** geçerli metin rengini gösteren bir değer. Bu değer -1 ise, Denetim metin rengini sistem rengi kullanıyor.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::SetTextColor](#settextcolor).  
  
##  <a name="setcheck"></a>  CTreeCtrl::SetCheck  
 Ağaç denetim öğesi için onay durumunu ayarlamak için bu üye işlevini çağırın.  
  
```  
BOOL SetCheck(
    HTREEITEM hItem,  
    BOOL fCheck = TRUE);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 **HTREEITEM** onay durumu değişikliği almak için.  
  
 `fCheck`  
 Ağaç denetim öğesi işaretli veya işaretsiz olup olmayacağını gösterir. Varsayılan olarak, `SetCheck` denetlenmesi için öğesini ayarlar.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 Ağaç denetim öğesi işaretli olduğunda ( `fCheck` kümesine **TRUE**), öğe ile bitişik bir onay işareti görünür.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#29](../../mfc/reference/codesnippet/cpp/ctreectrl-class_34.cpp)]  
  
### <a name="example"></a>Örnek  
 Onay kutularını kullanmak için ağaç denetimi doldurma önce TVS_CHECKBOXES ayarlayın.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl#30](../../mfc/reference/codesnippet/cpp/ctreectrl-class_35.cpp)]  
  
##  <a name="setextendedstyle"></a>  CTreeCtrl::SetExtendedStyle  
 Geçerli ağaç görünümü denetimi için genişletilmiş stiller ayarlar.  
  
```  
DWORD SetExtendedStyle(
    DWORD dwExMask,   
    DWORD dwExStyles);
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `dwExMask`|Geçerli ağaç görünümü denetiminde hangi stilleri bu yöntem tarafından etkilenen belirten bir bit maskesi. Bu parametre sıfırsa dikkate alınmaz ve değerini `dwExStyles` parametresi, ağaç görünümünde denetimine atanır.<br /><br /> Sıfır veya açıklanan stilleri Bitsel bir birleşimi (veya) belirtin [ağaç görünümü denetimi genişletilmiş stilleri](http://msdn.microsoft.com/library/windows/desktop/bb759981).|  
|[in] `dwExStyles`|Geçerli ağaç görünümünde hangi stilleri ayarlayın veya kaldırmak için Denetim belirten bir bit maskesi.<br /><br /> Stilleri bileşimini ayarlamak için açıklanan stilleri Bitsel bir birleşimi (veya) belirtmeniz [ağaç görünümü denetimi genişletilmiş stilleri](http://msdn.microsoft.com/library/windows/desktop/bb759981). Stilleri kümesi temizlemek için sıfır belirtin.|  
  
### <a name="return-value"></a>Dönüş Değeri  
 Önceki denetim stilleri genişletilmiş içeren bir değer.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu yöntem, belirtilen stillerini temizler `dwExMask` parametresi, ardından belirtilen stilleri ayarlar `dwExStyles` parametresi. Bitleri karşılık gelen genişletilmiş stilleri `dwExMask` değiştirin.  
  
 Bu yöntem gönderir [TVM_SETEXTENDEDSTYLE](http://msdn.microsoft.com/library/windows/desktop/bb773744) Windows SDK'ın açıklanan ileti.  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneği, bir değişken tanımlar `m_treeCtrl`, yani geçerli ağaç görünümü denetim erişmek için kullanılır. Kod örneği, imzasız tamsayı ve birkaç HTREEITEM değişkenleri de tanımlar. Bu değişkenler sonraki örnekte kullanılır.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneğinde ekler `TVS_EX_AUTOHSCROLL` genişletilmiş stili geçerli ağaç görünümü denetlemek için. Bir gösterilmez, kod örneği, önceki bölümde bir ağaç oluşan Amerika Birleşik Devletleri için bir kök ülke/bölge düğümü, alt düğümleri Pennsylvania ve Washington durumları için ve bu durumlar şehirlerde öğeleri ağacı görünümü oluşturduk. Otomatik odaklanmış ağaç öğesi görüntülemek için Kaydır gerekir böylece biz bilerek ağaç görünümü denetim dar yapılan.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#3](../../mfc/reference/codesnippet/cpp/ctreectrl-class_36.cpp)]  
  
##  <a name="setimagelist"></a>  CTreeCtrl::SetImageList  
 Normal ayarlamak için bu işlevi çağırmak veya ağacının durumu görüntü listesi denetimi görüntülemek ve yeni görüntüleri kullanarak denetimi yeniden.  
  
```  
CImageList* SetImageList(
    CImageList* pImageList,  
    int nImageListType);
```  
  
### <a name="parameters"></a>Parametreler  
 `pImageList`  
 İşaretçi atamak için resim listesi. Varsa `pImageList` olan **NULL**, tüm görüntüleri ağaç görünümü denetiminden kaldırılır.  
  
 `nImageListType`  
 Ayarlamak için resim listesi türü. Resim listesi aşağıdaki değerlerden biri olabilir:  
  
- `TVSIL_NORMAL` Ağaç görünümü öğesinin seçili ve nonselected görüntüleri içeren normal resim listesi ayarlar. Bu durum katmana görüntüler için kullanmanız gerekir.  
  
- `TVSIL_STATE` Kullanıcı tanımlı bir durumda olan ağaç görünümü öğeleri için görüntüleri içeren durumu resim listesi ayarlar.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Önceki resim listesi, varsa işaretçi; Aksi takdirde **NULL**.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::GetImageList](#getimagelist).  
  
##  <a name="setindent"></a>  CTreeCtrl::SetIndent  
 Denetimin yeni genişliğini yansıtacak şekilde yeniden boyutlandırmaya ve ağaç görünümü denetimi için girinti genişliğini ayarlamak için bu işlevini çağırın.  
  
```  
void SetIndent(UINT nIndent);
```  
  
### <a name="parameters"></a>Parametreler  
 `nIndent`  
 Girinti piksel cinsinden genişliği. Varsa `nIndent` küçük sistem tarafından tanımlanan en düşük genişliğinden, sistem tarafından tanımlanan en yeni genişliğini ayarlanır.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::GetIndent](#getindent).  
  
##  <a name="setinsertmark"></a>  CTreeCtrl::SetInsertMark  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_SETINSERTMARK](http://msdn.microsoft.com/library/windows/desktop/bb773753), Windows SDK'ın açıklandığı gibi.  
  
```  
BOOL SetInsertMark(
    HTREEITEM hItem,  
    BOOL fAfter = TRUE);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 **HTREEITEM** hangi öğesi ekleme işareti yerleştirilecek belirtir. Bu bağımsız değişken ise **NULL**, ekleme işareti kaldırılır.  
  
 *fAfter*  
 **BOOL** ekleme işareti önce veya belirtilen öğeden sonra yerleştirilir olmadığını belirten değer. Bu bağımsız değişkeni sıfır değilse, ekleme işareti öğeden sonra yerleştirilir. Bu bağımsız değişkeni sıfır ise, önce öğesi ekleme işareti yerleştirilir.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#31](../../mfc/reference/codesnippet/cpp/ctreectrl-class_37.cpp)]  
  
##  <a name="setinsertmarkcolor"></a>  CTreeCtrl::SetInsertMarkColor  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_SETINSERTMARKCOLOR](http://msdn.microsoft.com/library/windows/desktop/bb773755), Windows SDK'ın açıklandığı gibi.  
  
```  
COLORREF SetInsertMarkColor(COLORREF clrNew);
```  
  
### <a name="parameters"></a>Parametreler  
 `clrNew`  
 A **COLORREF** yeni ekleme işareti renk içeren değer.  
  
### <a name="return-value"></a>Dönüş Değeri  
 A **COLORREF** değeri önceki ekleme işareti renk içerir.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::GetInsertMarkColor](#getinsertmarkcolor).  
  
##  <a name="setitem"></a>  CTreeCtrl::SetItem  
 Belirtilen ağaç görünümü öğesi özniteliklerini ayarlamak için bu işlevini çağırın.  
  
```  
BOOL SetItem(TVITEM* pItem);

 
BOOL SetItem(
    HTREEITEM hItem,  
    UINT nMask,  
    LPCTSTR lpszItem,  
    int nImage,  
    int nSelectedImage,  
    UINT nState,  
    UINT nStateMask,  
    LPARAM lParam);
```  
  
### <a name="parameters"></a>Parametreler  
 `pItem`  
 Bir işaretçi bir [TVITEM](http://msdn.microsoft.com/library/windows/desktop/bb773456) yeni öğe içeren yapısı öznitelikleri, Windows SDK'ın açıklandığı gibi.  
  
 `hItem`  
 Ayarlanacak niteliklerini olan öğesinin tanıtıcısı. Bkz: **hItem** üyesi `TVITEM` Windows SDK'sındaki yapısı.  
  
 `nMask`  
 Ayarlamak için hangi özniteliklerin belirten tamsayı. Bkz: **maskesi** üyesi `TVITEM` yapısı.  
  
 `lpszItem`  
 Öğenin metni içeren bir dize adresidir.  
  
 `nImage`  
 Ağaç görünümü denetimin görüntü listesinde öğesi'nin görüntü dizini. Bkz: `iImage` üyesi `TVITEM` yapısı.  
  
 `nSelectedImage`  
 Ağaç görünümü denetimin görüntü listesinde seçilen görüntü öğenin dizini. Bkz: **iSelectedImage** üyesi `TVITEM` yapısı.  
  
 `nState`  
 Öğenin durumlar için değerler belirtir. Bkz: **durumu** üyesi `TVITEM` yapısı.  
  
 `nStateMask`  
 Hangi durumları için ayarlanacak belirtir. Bkz: **stateMask** üyesi `TVITEM` yapısı.  
  
 `lParam`  
 Bu öğeyle ilişkili 32-bit uygulamaya özgü bir değer.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 İçinde `TVITEM` yapısı, **hItem** üye öğesi tanımlar ve **maskesi** üye ayarlamak için hangi özniteliklerin belirtir.  
  
 Varsa **maskesi** üye veya `nMask` parametresi belirtir `TVIF_TEXT` değeri **pszText** üye veya `lpszItem` null sonlandırılmış bir dize ve adresidir**cchTextMax** üye göz ardı edilir. Varsa **maskesi** (veya `nMask`) belirtir `TVIF_STATE` değeri **stateMask** üye veya `nStateMask` parametresi belirtir değiştirmek için hangi öğesi durumları ve **durumu**  üye veya `nState` parametresi bu durumlar için değerler içeriyor.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#32](../../mfc/reference/codesnippet/cpp/ctreectrl-class_38.cpp)]  
  
##  <a name="setitemdata"></a>  CTreeCtrl::SetItemData  
 Belirtilen öğeyle ilişkili 32-bit uygulamaya özgü değeri ayarlamak için bu işlevini çağırın.  
  
```  
BOOL SetItemData(
    HTREEITEM hItem,  
    DWORD_PTR dwData);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Alınacak verileri olan öğesinin tanıtıcısı.  
  
 `dwData`  
 Belirtilen öğe ilişkilendirilen bir 32 bit uygulamaya özgü değer `hItem`.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#33](../../mfc/reference/codesnippet/cpp/ctreectrl-class_39.cpp)]  
  
##  <a name="setitemexpandedimageindex"></a>  CTreeCtrl::SetItemExpandedImageIndex  
 Belirtilen öğe geçerli ağaç görünümü denetiminin genişletilmiş durumdayken görüntülenecek resim dizinini ayarlar.  
  
```  
BOOL SetItemExpandedImageIndex(
    HTREEITEM hItem,   
    int iExpandedImage);
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `hItem`|Bir ağaç görünümü denetimi öğesi işler.|  
|[in] `iExpandedImage`|Belirtilen öğe genişletilmiş durumdayken görüntülenecek resim dizini.|  
  
### <a name="return-value"></a>Dönüş Değeri  
 `true` Bu yöntem başarılı olursa; Aksi takdirde `false`.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu yöntem gönderir [TVM_SETITEM](http://msdn.microsoft.com/library/windows/desktop/bb773758) Windows SDK'ın açıklanan ileti. Bu yöntem atar `iExpandedImage` parametresi `iExpandedImage` üyesi bir [TVITEMEX](http://msdn.microsoft.com/library/windows/desktop/bb773459) yapısı ve iletide yapısı kullanır.  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneği, bir değişken tanımlar `m_treeCtrl`, yani geçerli ağaç görünümü denetim erişmek için kullanılır. Kod örneği, imzasız tamsayı ve birkaç HTREEITEM değişkenleri de tanımlar. Bu değişkenler sonraki örnekte kullanılır.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneğinde belirlemek Önemsiz sınamasıdır olup olmadığını [CTreeCtrl::GetItemExpandedImageIndex](#getitemexpandedimageindex) yöntemi tarafından belirlenen değeri döndürür [CTreeCtrl::SetItemExpandedImageIndex](#setitemexpandedimageindex) yöntemi. Bir gösterilmez, kod örneği, önceki bölümde bir ağaç oluşan Amerika Birleşik Devletleri için bir kök ülke/bölge düğümü, alt düğümleri Pennsylvania ve Washington durumları için ve bu durumlar şehirlerde öğeleri ağacı görünümü oluşturduk.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#8](../../mfc/reference/codesnippet/cpp/ctreectrl-class_40.cpp)]  
  
##  <a name="setitemheight"></a>  CTreeCtrl::SetItemHeight  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_SETITEMHEIGHT](http://msdn.microsoft.com/library/windows/desktop/bb773761), Windows SDK'ın açıklandığı gibi.  
  
```  
SHORT SetItemHeight(SHORT cyHeight);
```  
  
### <a name="parameters"></a>Parametreler  
 `cyHeight`  
 Ağaç görünümünde piksel yeni her öğenin yüksekliğini belirtir. Ardından bu bağımsız değişken Görüntü yüksekliği küçükse, Görüntü yüksekliği ayarlanır. Bu bağımsız değişken bile değilse, bunu aşağı yuvarlanacağı en yakın çift değer. Bu bağımsız değişkeni -1 ise, denetim varsayılan öğe yüksekliği kullanmaya döner.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Piksel cinsinden öğeleri önceki yüksekliği.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::GetItemHeight](#getitemheight).  
  
##  <a name="setitemimage"></a>  CTreeCtrl::SetItemImage  
 Görüntüleri bir öğesiyle ilişkilendirir.  
  
```  
BOOL SetItemImage(
    HTREEITEM hItem,  
    int nImage,  
    int nSelectedImage);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ayarlanacak olan görüntüdür öğesinin tanıtıcısı.  
  
 `nImage`  
 Ağaç görünümü denetimin görüntü listesinde öğesi'nin görüntü dizini.  
  
 `nSelectedImage`  
 Ağaç görünümü denetimin görüntü listesinde seçilen görüntü öğenin dizini.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 Her öğe bir ağaç görünümü denetiminin ilişkili eşlemli görüntüleri çifti olabilir. Görüntüleri öğenin etiketi sol tarafında görünür. Bir görüntü öğesi seçilir ve öğe seçilmediğinde diğer görüntülendiğinde görüntülenir. Örneğin, değil seçildiğinde bir öğe seçildiğinde açık bir klasörde ve kapalı klasörü görüntülenebilir.  
  
 Öğenin resmi ve ağaç görünümü denetimin resim listesi içinde seçilen görüntü dizini ayarlamak için bu işlevini çağırın.  
  
 Görüntüleri hakkında daha fazla bilgi için bkz: [Cımagelist](../../mfc/reference/cimagelist-class.md).  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::GetItemImage](#getitemimage).  
  
##  <a name="setitemstate"></a>  CTreeCtrl::SetItemState  
 Belirtilen öğenin durumunu ayarlar `hItem`.  
  
```  
BOOL SetItemState(
    HTREEITEM hItem,  
    UINT nState,  
    UINT nStateMask);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ayarlanacak durumu olan öğesinin tanıtıcısı.  
  
 `nState`  
 Öğe için yeni durum belirtir.  
  
 `nStateMask`  
 Hangi durumları değiştirilmesi gerektiğini belirtir.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 Durumları hakkında daha fazla bilgi için bkz: [CTreeCtrl::GetItem](#getitem).  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::GetItemState](#getitemstate).  
  
##  <a name="setitemstateex"></a>  CTreeCtrl::SetItemStateEx  
 Belirtilen öğenin Genişletilmiş durum geçerli ağaç görünümü denetiminde ayarlar.  
  
```  
BOOL SetItemStateEx(
    HTREEITEM hItem,   
    UINT uStateEx);
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `hItem`|Bir ağaç görünümü denetimi öğesi işler.|  
|[in] `uStateEx`|Öğesinin genişletilmiş durumu. Daha fazla bilgi için bkz: `uStateEx` üyesi [TVITEMEX](http://msdn.microsoft.com/library/windows/desktop/bb773459) yapısı.|  
  
### <a name="return-value"></a>Dönüş Değeri  
 `true` Bu yöntem başarılı olursa; Aksi takdirde `false`.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu yöntem gönderir [TVM_SETITEM](http://msdn.microsoft.com/library/windows/desktop/bb773758) Windows SDK'ın açıklanan ileti. Bu yöntem atar `uStateEx` parametresi `uStateEx` üyesi bir [TVITEMEX](http://msdn.microsoft.com/library/windows/desktop/bb773459) yapısı ve iletide yapısı kullanır.  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneği, bir değişken tanımlar `m_treeCtrl`, yani geçerli ağaç görünümü denetim erişmek için kullanılır. Kod örneği, imzasız tamsayı ve birkaç HTREEITEM değişkenleri de tanımlar. Bu değişkenler sonraki örnekte kullanılır.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]  
  
### <a name="example"></a>Örnek  
 Aşağıdaki kod örneğinde bir ağaç görünümü öğesi devre dışı durumuna ayarlar. Bir gösterilmez, kod örneği, önceki bölümde bir ağaç oluşan Amerika Birleşik Devletleri için bir kök ülke/bölge düğümü, alt düğümleri Pennsylvania ve Washington durumları için ve bu durumlar şehirlerde öğeleri ağacı görünümü oluşturduk. Bu kod örneği Pennsylvania düğümü devre dışı durumuna ayarlar.  
  
 [!code-cpp[NVC_MFC_CTreeCtrl_s1#7](../../mfc/reference/codesnippet/cpp/ctreectrl-class_41.cpp)]  
  
##  <a name="setitemtext"></a>  CTreeCtrl::SetItemText  
 Belirtilen öğenin metnini ayarlar `hItem`.  
  
```  
BOOL SetItemText(
    HTREEITEM hItem,  
    LPCTSTR lpszItem);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Ayarlanacak metni olan öğesinin tanıtıcısı.  
  
 `lpszItem`  
 Öğe için yeni metin içeren bir dize adresidir  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#34](../../mfc/reference/codesnippet/cpp/ctreectrl-class_42.cpp)]  
  
##  <a name="setlinecolor"></a>  CTreeCtrl::SetLineColor  
 Geçerli ağaç görünümü denetiminin çizgi rengini ayarlamak için bu üye işlevini çağırın.  
  
```  
COLORREF SetLineColor(COLORREF clrNew = CLR_DEFAULT);
```  
  
### <a name="parameters"></a>Parametreler  
 `clrNew`  
 Yeni çizgi rengi.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Önceki çizgi rengi.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu üye işlevi win32 ileti davranışını uygulayan [TVM_SETLINECOLOR](http://msdn.microsoft.com/library/windows/desktop/bb773764), Windows SDK'ın açıklandığı gibi.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#35](../../mfc/reference/codesnippet/cpp/ctreectrl-class_43.cpp)]  
  
##  <a name="setscrolltime"></a>  CTreeCtrl::SetScrollTime  
 Ağaç görünümü denetiminin maksimum kaydırma süresini ayarlamak için bu üye işlevini çağırın.  
  
```  
UINT SetScrollTime(UINT uScrollTime);
```  
  
### <a name="parameters"></a>Parametreler  
 *uScrollTime*  
 Yeni maksimum kaydırma, milisaniye olarak süre. Bu değer 100'den az ise olacaktır en fazla 100 yuvarlanır.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Önceki en büyük kaydırma, milisaniye olarak süre.  
  
### <a name="remarks"></a>Açıklamalar  
 Bu üye işlevi win32 ileti davranışını uygulayan [TVM_SETSCROLLTIME](http://msdn.microsoft.com/library/windows/desktop/bb773767), Windows SDK'ın açıklandığı gibi.  
  
##  <a name="settextcolor"></a>  CTreeCtrl::SetTextColor  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_SETTEXTCOLOR](http://msdn.microsoft.com/library/windows/desktop/bb773769), Windows SDK'ın açıklandığı gibi.  
  
```  
COLORREF SetTextColor(COLORREF clr);
```  
  
### <a name="parameters"></a>Parametreler  
 `clr`  
 A **COLORREF** yeni metin rengi içeren değer. Bu bağımsız değişkeni -1 ise, Denetim metin rengini sistem rengi kullanmaya döner.  
  
### <a name="return-value"></a>Dönüş Değeri  
 A **COLORREF** önceki metin rengi temsil eden bir değer. Bu değer -1 ise, Denetim metin rengini sistem rengi kullanıyordu.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#36](../../mfc/reference/codesnippet/cpp/ctreectrl-class_44.cpp)]  
  
##  <a name="settooltips"></a>  CTreeCtrl::SetToolTips  
 Bu üye işlevi Win32 ileti davranışını uygulayan [TVM_SETTOOLTIPS](http://msdn.microsoft.com/library/windows/desktop/bb773772), Windows SDK'ın açıklandığı gibi.  
  
```  
CToolTipCtrl* SetToolTips(CToolTipCtrl* pWndTip);
```  
  
### <a name="parameters"></a>Parametreler  
 `pWndTip`  
 Bir işaretçi bir [CToolTipCtrl](../../mfc/reference/ctooltipctrl-class.md) ağaç denetimi kullanacağı nesnesi.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Bir işaretçi bir [CToolTipCtrl](../../mfc/reference/ctooltipctrl-class.md) önceden denetimi tarafından kullanılan araç ipucu içeren bir nesne veya **NULL** hiçbir araç ipuçları önceden kullanıldıysa.  
  
### <a name="remarks"></a>Açıklamalar  
 Araç ipuçları kullanılacağını belirtmek **TVS_NOTOOLTIPS** stil oluşturduğunuzda `CTreeCtrl` nesnesi.  
  
### <a name="example"></a>Örnek  
  Örneğin bkz [CTreeCtrl::GetToolTips](#gettooltips).  
  
##  <a name="showinfotip"></a>  CTreeCtrl::ShowInfoTip  
 Belirtilen öğe için bilgi ipucu geçerli ağaç görünümü denetiminde görüntüler.  
  
```  
void ShowInfoTip(HTREEITEM hItem);
```  
  
### <a name="parameters"></a>Parametreler  
  
|Parametre|Açıklama|  
|---------------|-----------------|  
|[in] `hItem`|Ağaç görünümü öğesi denetimi için bir tanıtıcı. Daha fazla bilgi için bkz: `hItem` üyesi [TVITEMEX](http://msdn.microsoft.com/library/windows/desktop/bb773459) yapısı.|  
  
### <a name="remarks"></a>Açıklamalar  
 Araç ipuçları ve infotips arasındaki fark hakkında daha fazla bilgi için "Araç ipuçları ve Infotips" konusuna arama [Microsoft Developer Network](http://go.microsoft.com/fwlink/p/?linkid=56322).  
  
 Bu yöntem gönderir [TVM_SHOWINFOTIP](http://msdn.microsoft.com/library/windows/desktop/bb773779) Windows SDK'ın açıklanan ileti.  
  
##  <a name="sortchildren"></a>  CTreeCtrl::SortChildren  
 Ağaç görünümü denetimi verilen üst öğenin alt öğeleri alfabetik olarak sıralamak için bu işlevini çağırın.  
  
```  
BOOL SortChildren(HTREEITEM hItem);
```  
  
### <a name="parameters"></a>Parametreler  
 `hItem`  
 Sıralanacak alt öğelerinin olan üst öğesinin tanıtıcısı. Varsa `hItem` olan **NULL**, sıralama ağaç kökünden devam.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 `SortChildren` ağacı ile çalışmamasını; yalnızca en yakınındaki alt öğeleri `hItem` sıralanır.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#37](../../mfc/reference/codesnippet/cpp/ctreectrl-class_45.cpp)]  
  
##  <a name="sortchildrencb"></a>  CTreeCtrl::SortChildrenCB  
 Ağaç görünümü öğeleri öğeleri karşılaştırır bir uygulama tanımlı geri çağırma işlevini kullanarak sıralamak için bu işlevini çağırın.  
  
```  
BOOL SortChildrenCB(LPTVSORTCB pSort);
```  
  
### <a name="parameters"></a>Parametreler  
 *pSort*  
 İşaretçi bir [TVSORTCB](http://msdn.microsoft.com/library/windows/desktop/bb773462) yapısı.  
  
### <a name="return-value"></a>Dönüş Değeri  
 Başarılıysa sıfır olmayan; Aksi takdirde 0.  
  
### <a name="remarks"></a>Açıklamalar  
 Yapısı karşılaştırma işlevi **lpfnCompare**, negatif bir değer döndürmelidir ilk öğeyi ikinci gelmelidir, bir pozitif değer ilk öğeyi ikinci izlemeniz gereken ya da sıfır iki öğeyi eşdeğerdir.  
  
 `lParam1` Ve `lParam2` parametreleri karşılık **lParam** üyesi [TVITEM](http://msdn.microsoft.com/library/windows/desktop/bb773456) iki karşılaştırılan öğeleri için yapılandırın. `lParamSort` Parametresi karşılık gelen **lParam** üyesi `TV_SORTCB` yapısı.  
  
### <a name="example"></a>Örnek  
 [!code-cpp[NVC_MFC_CTreeCtrl#38](../../mfc/reference/codesnippet/cpp/ctreectrl-class_46.cpp)]  
  
 [!code-cpp[NVC_MFC_CTreeCtrl#39](../../mfc/reference/codesnippet/cpp/ctreectrl-class_47.cpp)]  
  
## <a name="see-also"></a>Ayrıca Bkz.  
 [MFC örnek CMNCTRL1](../../visual-cpp-samples.md)   
 [CWnd sınıfı](../../mfc/reference/cwnd-class.md)   
 [Hiyerarşi grafiği](../../mfc/hierarchy-chart.md)   
 [CImageList Sınıfı](../../mfc/reference/cimagelist-class.md)
